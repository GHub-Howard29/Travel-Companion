# 專案現況總覽

> 本文件只記錄目前有效狀態，不保留已被取代的過程資訊。
>
> 最後更新：2026-09-03
>
> 已發布 App 版本：V3.6.2；Git 版本標籤 `v3.6.2` 與手動部署已完成。

## 最新發布：V3.6.2

- 2026-09-01 已合併至 `main`（`e9333f5`）、建立並推送 Git 版本標籤 `v3.6.2`，Product Owner 已完成手動部署；發布狀態為「已發布」。
- 正式 App build 與公開 metadata 均為 V3.6.2；`minimumSupportedVersion: 3.6.2`、`forceUpdate: true`，本版為必要更新。
- 縮短行程天數時會逐 Day 顯示永久刪除摘要並二次確認；台灣時間進入行程結束翌日後，行程編輯者對歷史共用資料唯讀，管理者保留修改權。
- Supabase migration、回滾式角色驗證 9 項、lint、build、PWA 與瀏覽器安全驗證均已通過；發布後功能、桌面 Chrome／Edge／PWA 及 Android Chrome／PWA 實測通過，iOS Safari／standalone PWA 尚未測試。
- 發布後已確認舊版更新提示與更新流程正確；版本資訊卻把 V3.6.2 顯示為「一般更新」，已記錄為 BUG029，尚未修改程式。
- 詳細範圍見《24_V3.6.2_行程天數縮短刪除與歷史行程鎖定規格》。

## 下一個維護版：V3.6.3

- 定位為 V3.6.2 發布後前端修正版；實測結果與修正範圍已彙整，尚未開始程式實作。
- 已確定納入 BUG029：分離更新提示的即時必要性判定與版本資訊保存的發布政策，修正 V3.6.2 錯顯示為一般更新。
- 同版處理交通圖示、管理者歷史提醒時機，以及縮短天數兩階段不同文案確認。
- 歷史唯讀參與者與跨裝置 Trip 資料變動提醒因涉及角色、快取、RLS、Realtime 與未儲存資料風險，已拆至 V3.6.4。
- iOS Safari／standalone PWA 保留為本版發布前補驗；目前尚未修改 V3.6.3 程式或版本 metadata。
- V3.6.3 是否必要更新尚未決定，待需求確認與實作風險評估後由 Product Owner 確認。
- 詳細範圍見《25_V3.6.3_V3.6.2發布後前端修正規格》。

## 後續維護版：V3.6.4

- 「歷史唯讀參與者」保留完整歷史共用資料讀取能力、不顯示編輯者專用鎖定提醒，並禁止所有歷史共用資料寫入；此狀態不是 User，也不變更全域角色。
- 跨裝置 Trip 資料變動只提供手動刷新提醒，不自動重載或捨棄編輯中資料。
- 資料變動提醒的事件、UI、刷新、未儲存狀態、離線備援、Realtime／Broadcast、隱私、migration、相容性與更新政策，必須在開發前再次討論及核准。
- 詳細範圍見《26_V3.6.4_歷史唯讀參與者與跨裝置資料變動提醒規格》。

## 後續效能維護版：V3.6.5

- Product Owner 已於 2026-09-03 核准兩項改善方向，尚未實作：資料 ready 後立即移除靜態啟動畫面，以及同次初始化共用 Trip 雲端快照。
- 可移除量測已確認 V3.6.2 單次點擊更新的 Worker 接管約 38 ms，並非本次主要慢點；更新後首次 navigation 約有 1 秒瀏覽器／Service Worker 等待，本版不以取消 reload 或繞過接管處理。
- 本機桌面樣本另確認 Trip metadata／detail 依序重複讀取雲端 Trip，且資料 ready 後仍因 lazy 頁面等待約 0.31 秒才移除啟動畫面。
- 改善不得變更角色、RLS、離線、pending、跨裝置同步、更新提示或單次接管安全流程；完整規格見《27_V3.6.5_PWA更新後介面顯示效能改善規格》。

## 前一版：V3.6.1

- 2026-09-01 已合併至 `main`、建立帶註解 Git 版本標籤 `v3.6.1` 並完成部署；正式 App build 與公開 metadata 均為 V3.6.1，採非強制更新。
- Android 手機由 V3.6.0 更新時，第一次點擊即可完成更新，不需第二次點擊；記帳金額輸入欄位不再變形或超出畫面。
- iOS 與桌面瀏覽器／PWA 仍保留跨平台補驗證。
- 發布後觀察到的更新後介面顯示偏慢已完成初步量測定位；兩項低風險改善經核准並排入 V3.6.5，既有單次點擊更新修正維持不變。

## 較早版本：V3.6.0

- 2026-08-30 已合併至 `main`（`0cce637`）、建立帶註解 Git 版本標籤 `v3.6.0` 並部署 GitHub Pages（gh-pages `f16e0dca`）。
- 正式 `app-version.json` 為 `3.6.0`、`minimumSupportedVersion: 3.5.6`、`forceUpdate: true`；正式首頁引用 V3.6.0 build，舊 Service Worker 分頁關閉後新版已接管並顯示 V3.6.0。
- 地點確認、相鄰卡片路線、三種交通方式、Google Maps 外部路線與管理模式交通修改已上線；正式 Supabase migration、Function、Google Cloud key 限制／配額／US$5 預算與瀏覽器金鑰邊界均已驗證。
- 正式站 Guest 行程與已存路線正常；完整角色矩陣、正式站 OAuth 自動化登入及 Android／iOS 實機列為發布後補驗證。

## 版本更新政策基準：V3.5.6

- Product Owner 已於 2026-08-28 完成 V3.5.6 合併、建立 Git 版本標籤 `v3.5.6` 並部署；發布 metadata 為 `3.5.6`、`minimumSupportedVersion: 3.5.6`，橋接期間保留 `forceUpdate: true`。
- 新版以最低支援版本判定必要更新，舊版 App 仍因橋接旗標被要求升級；已達最低版本的新版可在未來一般更新選擇稍後更新。
- 資訊卡片內容與每日行程說明的選色工具列移至輸入框下方；Android／iOS 的原生選取、局部套色及相關發布前驗證已通過。
- 行程時間支援半形／全形冒號並正規化為 `HH:mm`，拒絕冒號旁空白；同一天離開時間不得早於到達時間。

## 後續功能版：V3.7.0

- 2026-08-31 已完成「當日後續行程時間連動調整」開發內容、細節與互動模擬定案；目前尚未修改程式。
- 功能只由 Day 管理的單次「調整時間」模式啟動；一般卡片編輯仍只更新目前卡片並顯示既有衝突警告。
- 逐行預覽會交錯顯示景點時間及路線交通方式／時間／距離；每站推算到達時間進位至下一個 `00` 或 `30` 分刻度，整批確認後才更新當日後續時間。
- 開車、步行、大眾運輸圖示將採自託管 Google Material Symbols，不擷取 Google Maps 專屬品牌資產。
- 資料或計算完整性錯誤一律阻擋整批套用；仍可安全計算的時間衝突／緩衝提醒會顯示於預覽，由管理者明確確認。
- 詳細範圍與安全規則見《22_V3.7.0_當日後續行程時間連動調整規格》。

## 目前能力

- 以 Trip 為核心，提供每日行程、共同／私人清單、其他資訊、多人旅費帳本、附件、Excel 匯出及外幣換算。
- React／TypeScript／Vite PWA，非首屏 Travel Tool 與 Excel 採按需載入。
- 使用 localStorage、IndexedDB 與 Supabase 實作 Offline First、雲端同步及附件管理。
- 角色包含 Guest、User、`trip_editor`、`super_admin`；目前權限矩陣見《13_權限設定資料表》。
- PWA 具版本更新提示；一般更新可稍後，強制更新不可稍後。

## 既有發布與安全基準

- 已於 2026-08-26 合併、建立 Git 版本標籤 `v3.5.3` 並發布；採非強制更新。
- 正式版加入 Content Security Policy、`no-referrer`、OAuth 等待畫面、外部 URL 與新視窗安全處理，且保持附件、PWA、Excel、分享與既有權限／同步資料流相容。
- 正式 build、OAuth、PWA、Service Worker、附件、Excel、分享與外部連結回歸，以及正式站新版本更新提示均已通過；iOS standalone PWA Google OAuth 保留日後補驗證。

## 前一版：V3.5.1

- `expense-attachments` 已收斂為 private bucket，Trip／expense ownership Storage RLS、附件路徑檢查、15 分鐘簽名網址及中文舊 Trip ID 相容修正均已部署。
- 已完成首頁分享入口與浮動分享視窗；未變動既有 App 文案／小圖示，未新增更多按鈕、單一 Trip 分享網址或敏感附件可見層級。
- BUG026 Realtime、共同清單競態、角色限制、宜蘭附件重試與 Storage 四角色 RLS 回歸均已通過；V3.5.1 於 2026-08-26 發布並結案。

## 最新資料庫維護：V3.5.2（無獨立 App 發布）

- 已建立向前 migration，將兩個 `SECURITY DEFINER` 權限查詢核心移至非暴露 `private` schema，`public` 同名函式改為 `SECURITY INVOKER` 相容包裝。
- 正式資料庫共有 13 個 `tc_` 公開函式；migration 已將其全部固定為空 `search_path`，撤銷 `PUBLIC` 隱含執行與 trigger-only API 執行權，並設定未來函式預設拒絕公開執行。
- `public` schema 的 API 角色 CREATE 權已確認為關閉，migration 會再次明確撤銷並以斷言防止回歸。
- migration 已正式套用（Supabase migration `20260826083326`）；13 個公開函式的 catalog 驗證、核心角色／RLS 16 項與 Storage 13 項回歸全部通過，fixture 均已 `ROLLBACK`。
- 套用後 advisors 已確認本版原有的 5 個 mutable `search_path` 與 4 個公開 `SECURITY DEFINER` 警告全部清除；剩餘 Auth 密碼保護與 checklist 效能／索引建議不屬本版範圍。
- 本版不改前端、角色矩陣、Trip 瀏覽制度或 `allowed_roles` 規則。
- Product Owner 已確認不建立 V3.5.2 App build、Git 版本標籤或版本 metadata；V3.5.1 前端維持相容，資料庫 migration 與完成紀錄保留。

## 前一版：V3.4.11

- 原生 Splash、HTML 等待底圖與 App 系統資訊列已統一為 `#FFF3E8`。
- 保留單次 Android 原生圖示，等待階段不顯示第二張網頁圖示，首個功能頁完成後才切入操作介面。
- Git 版本標籤 `v3.4.11` 保留為 V3.5.0 的上一個正式發布基準，Android PWA 實機驗證通過。
- 實際視覺效果尚未完全符合 Product Owner 期待，但本階段不再追加啟動畫面修正。
- 本版沒有 Supabase schema、RLS、migration 或 Trip 隱私變更，`FORCE_UPDATE = false`。

## 前一版：V3.5.0

- 已完成 `expense-attachments` private bucket、Trip／expense ownership Storage RLS、附件路徑檢查與 15 分鐘簽名網址。
- 已完成側邊首頁關閉鈕左側分享入口、分享浮動視窗與固定 App 首頁原生分享；未變動既有文案、小圖示，未新增更多按鈕或單一 Trip 分享網址。
- 已完成 BUG026 Realtime 訂閱與 RLS-safe refetch；保留 focus／visibility／reconnect 及 pending 佇列保護。
- `npm run lint`、`npm run build` 與版本歷史驗證已通過；正式 Supabase migration、private bucket、Storage RLS 與 Realtime publication 已驗證。Guest／User 操作介面阻擋、`trip_editor` 建立／修改範圍、雙端跨裝置即時刷新（更新延遲不超過 1 秒）、共同清單兩帳號競態（最後成功寫入不回滾）、宜蘭行程兩筆失敗附件重試，以及 anon／其他 Trip editor／所屬 Trip editor／super admin 的 Storage 四角色 RLS 回歸均已通過。
- 不新增敏感附件可見層級。

## V3.5.1 實作邊界

- 維持現行角色與 Trip 瀏覽制度。
- V3.5.1 延續已確認的模擬圖、規格與修改邊界，已新增頁首小型分享圖示與浮動分享視窗；既有 App 文案與小圖示未變動。
- V3.5.1 已修正其他資訊、領隊／導遊資料及共同準備清單跨裝置即時刷新缺失，保留既有同步佇列與角色權限。
- 分享視窗顯示本專案圖示、首頁超連結、分享連結文字及每行四個原生分享 App 圖示，超出視窗時可上下滑動。
- 原生分享由使用者選擇通訊軟體頻道或其他系統分享目標；保留既有關閉按鈕返回當下主畫面，不新增「更多」按鈕、不建立單一 Trip 分享網址。
- Supabase Storage 附件安全維護未新增敏感附件可見層級。
- 所有新增或調整畫面必須先提供模擬圖，經 Product Owner 確認後才能修改程式。

## 後續順序

1. V3.5.5 行程返回、外部連結與資訊文字醒目。
2. V3.6.0 地點間預估移動資訊。

## 安全方向相容性

- V3.5.1 只做 Storage bucket、Storage RLS、附件路徑與簽名網址安全維護，不新增敏感附件可見層級，與 V3.5.0 相容。
- V3.5.2 只硬化資料庫安全函式與公開執行權限，不改變 V3.5.0 已定案的角色矩陣與 `allowed_roles` 規則。
- V3.5.3 已完成 CSP、Referrer Policy、URL／外部視窗及 OAuth DOM 程式修正；GitHub Pages 無法自訂的 Permissions Policy、CSP Report-Only 與 `frame-ancestors` header 已明確列為託管限制。正式 build、OAuth、PWA、Service Worker、附件、Excel 與分享外部連結回歸已通過；iOS standalone PWA OAuth 保留日後補驗證。

未完成項目見《09_待辦事項_TODO》，版本範圍見《02_產品開發路線圖》。
