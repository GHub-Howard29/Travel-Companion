# 專案現況總覽

> 本文件只記錄目前有效狀態；已發布歷史以《07_版本更新紀錄》為準，未完成工作以《09_待辦事項_TODO》為準。
>
> 最後更新：2026-09-14

## 正式版本

- 已發布 App 版本：V3.9.0；`main`、帶註解標籤 `v3.9.0`、GitHub Pages、正式 metadata、Service Worker、版本資訊與 Guest smoke 均已完成發布驗證。
- 正式更新政策為一般更新，`minimumSupportedVersion: 3.8.2`、`forceUpdate: false`。
- V3.9.0 新增 Google 地點搜尋候選照片，以及 Commons 每日行程卡片管理者選圖與「換一批」；production Function version 9 為 ACTIVE。

## 目前能力

- 以 Trip 為核心，提供每日行程、共同／私人清單、其他資訊、多人旅費帳本、附件、Excel 匯出及外幣換算。
- 每日行程支援到達／離開時間、相鄰地點交通方式／時間／距離、Google Maps 外部開啟，以及 V3.7.0 單次後續時間調整。
- React／TypeScript／Vite PWA；使用 localStorage、IndexedDB 與 Supabase 實作 Offline First、雲端同步及附件管理。
- 角色包含 Guest、User、`trip_editor`、`super_admin`；權限矩陣以《13_權限設定資料表》為準。
- Trip 主資料寫入使用遠端版本檢查，跨裝置版本衝突時不覆蓋遠端資料。

## 最新發布能力

- 地點搜尋候選會顯示 Google Place Photos 索引照、地址及地圖連結，只供當次地點確認，不寫入 Trip。
- 每日行程管理者可搜尋、換批並選用 Wikimedia Commons 照片；保存 WebP 與來源、作者、授權資訊，跨日複製沿用同一引用。
- 每日卡片照片只占實際高度，後續說明向左回流；作者／授權文字與地圖按鈕上緣對齊。桌面與 390×844 正式站驗證通過。
- 跨日複製開啟時顯示來源抵達／離開時間，但新副本時間預設空白且兩欄必填；支援半形／全形冒號並正規化為 `HH:MM`。
- 一次選擇多個目標 Day 時共用同一組新時間，每份副本建立獨立 ID 並依新抵達時間穩定插入；相同時間放在既有同時段卡片之後。
- 目標 Day 既有卡片不重新排序；插入點前一張卡片若改變目的地，只清除 `travelToNext` 並保留交通方式偏好。
- 多個 Day 以一次 Trip 寫入，失敗或遠端版本衝突時不產生部分副本；成功後留在來源 Day。
- V3.8.2 已完成領域測試、TypeScript、lint、完整 build、桌面／390×844 驗證與正式站版本資訊 smoke；發布結果見《43_V3.8.2_正式發布操作手冊》。

## 目前開發方向

- V3.9.0 已完成 Google 照片路徑與成本盤點，但原報告遺漏免費圖庫；原「只採管理者手動封面」結論已撤回。
- Commons／Openverse 已完成 12 地點、24 次公開 API 查詢：兩者 hit@3 均為 7／12，知名景點可用但租車點與四家餐廳皆未命中；候選照片暫態資料契約亦已通過。
- Google 真實 spike 已完成：5 組搜尋共 10 個候選，10／10 有照片且成功載入，包含免費來源找不到的租車點與餐廳；只使用 10／25 次核准照片請求，暫時性 Edge Function 已刪除並確認 404。
- Product Owner 已確認 V3.9.0 同時採 Google 搜尋候選暫態索引照與 Commons 每日卡片管理者選圖；Commons 中文／英文、授權及容量補充 spike 已完成。
- V3.9.0 桌面與 390×844 UI、可見文案及跨日照片引用規則已核准；前端、Edge Function、Storage migration、領域測試與本機 build 已完成。
- V3.9.0 migration 已在本機 Docker 隔離 Postgres 實際套用；Storage RLS、月額度原子性與瀏覽器角色隔離測試通過，Supabase security／performance advisors 為零問題，Edge Runtime 可載入 `travel-route`。
- V3.9.0 正式 metadata 為一般更新：`minimumSupportedVersion: 3.8.2`、`forceUpdate: false`；遠端隔離 Supabase CI run `34688235879` 已通過且無 annotations。
- V3.9.0 production backend 已完成 logical backup、migration `20260912093527`、production-safe postflight、advisors 與 `travel-route` 部署；兩個新增 action 的未登入請求均為 403。2026-09-13 已將 Commons 縮圖 host 與「換一批」分頁修正部署至 Function version 9（ACTIVE、`verify_jwt=false`），並修正 Supabase Storage 圖片 CSP。
- 正式管理者已完成 Google 候選、Commons 選圖、Storage 保存與重新整理 smoke；每日卡片照片改為只占實際高度，後續說明文字向左回流，作者／授權文字與地圖按鈕上緣對齊且維持原字級及左側間距。桌面與 390×844 驗證通過；重新載入後目前頁面已無 smoke 照片，不自行新增或恢復 production 照片。
- V3.9.0 已於 2026-09-13 正式發布；`main` 與 `v3.9.0` 指向 `7abac83`，GitHub Pages 部署為 `d7b8e93`，正式站顯示 v3.9.0 且 Guest 行程載入正常。
- V3.10.0 使用紀錄仍未定案；原逐次明細、App 查詢頁、獨立角色與 TOTP 方案維持先前草案，不併入 V3.9.0。
- V3.9.1 第一優先為已核准的 Commons 實體導向候選照片精準篩選：以 Wikidata／Commons 證據、硬篩、可解釋排序、人工 `needs-review` 與 `no-suitable-image` 改善代表性；獲准受限的單一 Wikimedia 順序作業例外、請求預算、快取、AI 手動換詞、選圖／裁切／放大檢視 UI 與無障礙。A「中文搜尋自動擴充英文關鍵詞」與 C「已確認 MAP 地點脈絡輔助搜尋」已定案停用；B「Commons／Pexels／Pixabay 來源切換」保留為第二候選方案，其八項來源策略、UI／文案、無障礙與額度治理已定案。API key、真實矩陣、工程設計與程式實作仍須另行授權。詳見《48》、《49》及《50_V3.9.1_Commons實體導向候選照片精準篩選決策規格》，且不變更既有 V3.10.0 規劃。
- V3.9.1 另排入跨日複製時間欄位的可用性修正：接受有效四碼數字自動格式化為 `HH:MM`，修正行動裝置數字鍵盤無法輸入冒號。此項不涉及資料格式或外部 API；桌面與 390×844 UI、提交警告跳窗與無障礙行為均已核准，仍待實作授權；詳見《51_V3.9.1_跨日複製四碼時間輸入修正規格》。
- 排序後全日時間整合、預約紀錄、旅行日誌、保險資訊與 AI 助手維持未排程。

## 開發與發布邊界

- 所有新增或調整畫面必須先提供模擬圖，經 Product Owner 確認後才能修改程式。
- 未經 Product Owner 明確要求，不執行正式 Supabase migration、部署、合併、推送或建立 tag。
- 已發布 migration、版本 metadata、版本歷史與 Git tag 不回寫；問題一律以新版本向前修復。
- 修改完成後至少執行與風險相稱的純函式測試、TypeScript、lint、完整 build 與必要實機回歸。

## 尚待處理

- V3.9.1 子方案 B 已核准八項來源策略與技術邊界，仍需完成 API key、真實矩陣、UI／文案、無障礙、額度治理與實作驗收討論；A、C 仍需完成來源策略、成本、授權、資料契約、UI 與文案討論。MAP 脈絡方案另須實測命中率並計算 Google API 額度與最壞成本；各項完整核准前禁止實作、正式 API 呼叫擴增及 UI 製作。
- V3.9.1 另列既有介面改善：Google Maps 連結須另開候選地點的實際 Maps 地點頁而非照片頁，並改稱「查看地點資訊」；MAP 候選不理想時使用已核准文案提示調整地點關鍵字；Commons 每批由 8 張改為 6 張。尚未核准實作。
- V3.8.2 的 Android／iOS 更新接管、正式管理者多日複製與多裝置衝突列為發布後補驗。
- V3.8.1 的實體裝置、PWA 冷啟動離線及舊版必要更新流程仍待補驗；完整結果見《41_V3.8.1_實作與驗證紀錄》。
- iOS／Android 與兩台實體裝置仍有跨版本發布後補驗證，完整清單見《09_待辦事項_TODO》。
- 離線載入效能、帳本附件管理與其他資料的合併策略只列改善候選，須先量測或取得具體需求，不直接進入開發。
