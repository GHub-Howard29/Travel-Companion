# 專案現況總覽

> 本文件只記錄目前有效狀態；已發布歷史以《07_版本更新紀錄》為準，未完成工作以《09_待辦事項_TODO》為準。
>
> 最後更新：2026-09-10

## 正式版本

- 已發布 App 版本：V3.8.0；`main`、帶註解標籤 `v3.8.0`、GitHub Pages、正式 metadata、主程式與行程功能資產均已完成發布驗證。
- 正式更新政策為必要更新，`minimumSupportedVersion: 3.8.0`、`forceUpdate: true`。
- V3.8.0 已新增每日行程排序與同旅程跨日多選複製；未變更 Supabase schema、RLS、Edge Function、Storage 或既有行程資料。

## 目前能力

- 以 Trip 為核心，提供每日行程、共同／私人清單、其他資訊、多人旅費帳本、附件、Excel 匯出及外幣換算。
- 每日行程支援到達／離開時間、相鄰地點交通方式／時間／距離、Google Maps 外部開啟，以及 V3.7.0 單次後續時間調整。
- React／TypeScript／Vite PWA；使用 localStorage、IndexedDB 與 Supabase 實作 Offline First、雲端同步及附件管理。
- 角色包含 Guest、User、`trip_editor`、`super_admin`；權限矩陣以《13_權限設定資料表》為準。
- Trip 主資料寫入使用遠端版本檢查，跨裝置版本衝突時不覆蓋遠端資料。

## 最新發布能力

- V3.8.0「每日行程排序與跨日複製」已縮減為 MVP，避免重複建立 V3.7.0 的時間調整引擎。
- V3.8.0 以卡片穩定 ID 與每日陣列位置保存順序；不新增 `sortOrder`，不做全旅程資料遷移。
- 排序只保存新順序並清除失效路線，原到達／離開時間保留；需要時由使用者接續進入既有「調整時間」。
- 跨日複製支援同一旅程多選 Day、保留內容與時間、產生新 ID、依時間插入，不複製相鄰路線資訊。
- Product Owner 已於 2026-09-10 確認縮減版桌面／手機模擬圖；同日已完成穩定 ID、排序草稿、跨日多選複製、路線快取失效、本機純函式測試、完整 build 與桌面／手機預覽驗證。
- 尚待登入角色、歷史行程、離線阻擋、跨裝置版本衝突與實機必要更新流程補驗。
- Product Owner 已確認 V3.8.0 採必要更新；正式發布日期為 `2026-09-10`，metadata 為 `minimumSupportedVersion: 3.8.0`、`forceUpdate: true`。
- Product Owner 已完成手動發布；`main` 與 `v3.8.0` 發布點為 `25def61`，`develop` 另含發布結果文件提交，正式站資產與版本政策已驗證。

## 後續方向

- 目前開發版本為 V3.8.1「Trip 刪除墓碑與唯一識別」；資料方案、版本、必要更新政策、靜默切換與種子保護介面均已確認，候選程式與 migration 已完成。
- V3.8.1 候選以永久最小墓碑清除跨裝置已刪除 Trip 殘留，新建 Trip 改用 UUID，使相同日期與模式的歷史資料可作為新 Trip 重建；候選 metadata 為 `minimumSupportedVersion: 3.8.1`、`forceUpdate: true`，尚未修改正式 metadata。
- 本版不新增同步提示，採背景靜默校正；目前 Trip 被刪除後固定開啟預設 Trip 的每日詳細行程及適合 Day。桌面／手機模擬圖已於 2026-09-10 核准。
- `free-travel-2026-01` 與 `group-tour-2026-10` 固定為可編輯但不可刪除的系統保留 Trip；既有預設 Trip 選擇規則不變。
- V3.8.2 已排定跨日複製時間設定；兩欄空白必填、多日共用一組時間、依新抵達時間插入的桌面／手機 UI 已核准，待 V3.8.1 後實作。
- 排序後自動時間整合退回跨版本候選，只有實際需求成立時才重新排程。
- V3.9.0 卡片照片先做來源、授權、快取、效能、離線與成本可行性驗證，不視為已定案功能。
- V3.10.0 使用紀錄重新縮小範圍；原逐次明細、App 查詢頁、獨立角色與 TOTP 方案改列先前草案，須重新確認後才能實作。
- 預約紀錄、旅行日誌、保險資訊與 AI 助手維持未排程。

## 開發與發布邊界

- 所有新增或調整畫面必須先提供模擬圖，經 Product Owner 確認後才能修改程式。
- 未經 Product Owner 明確要求，不執行正式 Supabase migration、部署、合併、推送或建立 tag。
- 已發布 migration、版本 metadata、版本歷史與 Git tag 不回寫；問題一律以新版本向前修復。
- 修改完成後至少執行與風險相稱的純函式測試、TypeScript、lint、完整 build 與必要實機回歸。

## 尚待處理

- V3.8.1 候選程式、向前 migration、獨立 baseline、17 項本機 SQL 角色／交易驗證、database advisors、rollback／重套、專項測試、TypeScript、lint 與完整 build 已完成；尚待手動 GitHub Actions Auth／PostgREST／Realtime 閘門，以及離線、雙裝置、實機 UI 與必要更新驗證。完整結果見《41_V3.8.1_實作與驗證紀錄》。
- iOS／Android 與兩台實體裝置仍有跨版本發布後補驗證，完整清單見《09_待辦事項_TODO》。
- 離線載入效能、帳本附件管理與其他資料的合併策略只列改善候選，須先量測或取得具體需求，不直接進入開發。
