# 專案現況總覽

> 本文件只記錄目前有效狀態，不保留已被取代的過程資訊。
>
> 最後更新：2026-08-24
>
> 已發布版本：V3.4.10；目前待發布版本：V3.4.11；下一個功能版本：V3.5.0。

## 目前能力

- 以 Trip 為核心，提供每日行程、共同／私人清單、其他資訊、多人旅費帳本、附件、Excel 匯出及外幣換算。
- React／TypeScript／Vite PWA，非首屏 Travel Tool 與 Excel 採按需載入。
- 使用 localStorage、IndexedDB 與 Supabase 實作 Offline First、雲端同步及附件管理。
- 角色包含 Guest、User、`trip_editor`、`super_admin`；目前權限矩陣見《13_權限設定資料表》。
- PWA 具版本更新提示；一般更新可稍後，強制更新不可稍後。

## 最新發布：V3.4.10

- 已移除 Android 原生 Splash 後重複顯示的網頁層 App 圖示。
- 原生圖示結束後維持純色遮罩，首個功能頁完成後才切入操作介面。
- `main`、`v3.4.10` tag 與 GitHub Pages 部署版本均為 V3.4.10。
- 本版沒有 Supabase schema、RLS、migration 或 Trip 隱私變更，`FORCE_UPDATE = false`。

## 目前待發布：V3.4.11

- 原生 Splash、HTML 等待底圖與 App 系統資訊列統一為 `#FFF3E8`。
- 保留單次原生圖示與等待遮罩，降低 HyperOS 上下系統區域與載入底圖的色差。
- 無資料庫變更，`FORCE_UPDATE = false`；待合併、建立 tag、部署與 Android PWA 實機複驗。

## 下一個功能版本：V3.5.0

- 新增 Trip 公開／私人設定與 Guest 存取模型。
- UI、`is_public` migration、RLS、REST、靜態 Trip 移除及帳號隔離快取必須同版發布。
- 既有與新建 Trip 預設私人；只有 `super_admin` 可切換公開狀態。
- 目前建議 `FORCE_UPDATE = true`，發布前最終確認。
- 正式 migration 前必須備份、隔離演練並完成多角色與直接網址驗證。

## 後續順序

1. V3.5.1 Supabase Storage 附件權限。
2. V3.5.2 Supabase 資料庫權限函式硬化。
3. V3.5.3 前端瀏覽器安全防護（暫定，開始前重審）。
4. V3.6.0 地點間預估移動資訊。

未完成項目見《09_待辦事項_TODO》，版本範圍見《02_產品開發路線圖》。
