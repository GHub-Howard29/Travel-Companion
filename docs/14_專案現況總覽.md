# 專案現況總覽

> 本文件只記錄目前有效狀態；已發布歷史以《07_版本更新紀錄》為準，未完成工作以《09_待辦事項_TODO》為準。
>
> 最後更新：2026-09-18

## 正式版本

- 已發布 App 版本：V3.9.3；production migration、`main`／`develop`、annotated tag `v3.9.3` 與 GitHub Pages 均已完成。
- V3.9.3 採一般更新，`minimumSupportedVersion: 3.9.2`、`forceUpdate: false`；正式 metadata 與線上 `app-version.json` 均已確認為 3.9.3。
- 唯一系統開發者 capability、最小彙總使用紀錄、側邊欄 UI、驗證聚合、App TypeScript 與冷啟動埋點已正式上線；發布提交為 `3e92c22`。

## 目前能力

- 以 Trip 為核心，提供每日行程、共同／私人清單、其他資訊、多人旅費帳本、附件、Excel 匯出及外幣換算。
- 每日行程支援到達／離開時間、相鄰地點交通方式／時間／距離、Google Maps 外部開啟，以及 V3.7.0 單次後續時間調整。
- React／TypeScript／Vite PWA；使用 localStorage、IndexedDB 與 Supabase 實作 Offline First、雲端同步及附件管理。
- 角色包含 Guest、User、`trip_editor`、`super_admin`；權限矩陣以《13_權限設定資料表》為準。
- Trip 主資料寫入使用遠端版本檢查，跨裝置版本衝突時不覆蓋遠端資料。

## 最新發布能力

- 地點搜尋候選會顯示 Google Place Photos 索引照、地址及地圖連結，只供當次地點確認，不寫入 Trip。
- 每日行程管理者可搜尋、換批並選用 Wikimedia Commons 照片；保存 WebP 與來源、作者、授權資訊，跨日複製沿用同一引用。
- 每日卡片照片只占實際高度，後續說明向左回流；作者／授權文字與地圖按鈕上緣對齊。桌面與 390×844 正式站驗證通過。
- 跨日複製開啟時顯示來源抵達／離開時間，但新副本時間預設空白且兩欄必填；支援半形／全形冒號並正規化為 `HH:MM`。
- 一次選擇多個目標 Day 時共用同一組新時間，每份副本建立獨立 ID 並依新抵達時間穩定插入；相同時間放在既有同時段卡片之後。
- 目標 Day 既有卡片不重新排序；插入點前一張卡片若改變目的地，只清除 `travelToNext` 並保留交通方式偏好。
- 多個 Day 以一次 Trip 寫入，失敗或遠端版本衝突時不產生部分副本；成功後留在來源 Day。
- V3.8.2 已完成領域測試、TypeScript、lint、完整 build、桌面／390×844 驗證與正式站版本資訊 smoke；發布結果見《43_V3.8.2_正式發布操作手冊》。

## 目前開發方向

- V3.9.3 已正式發布；完整本機、production、Git 與部署紀錄見《55》。
- V3.9.4 已立案處理 Commons 照片搜尋額度不足、候選數少與請求效率；目前只完成計畫文件，不修改照片搜尋程式或額度，也未發出新的外部請求。詳見《56》。

- V3.9.0 已完成 Google 照片路徑與成本盤點，但原報告遺漏免費圖庫；原「只採管理者手動封面」結論已撤回。
- Commons／Openverse 已完成 12 地點、24 次公開 API 查詢：兩者 hit@3 均為 7／12，知名景點可用但租車點與四家餐廳皆未命中；候選照片暫態資料契約亦已通過。
- Google 真實 spike 已完成：5 組搜尋共 10 個候選，10／10 有照片且成功載入，包含免費來源找不到的租車點與餐廳；只使用 10／25 次核准照片請求，暫時性 Edge Function 已刪除並確認 404。
- Product Owner 已確認 V3.9.0 同時採 Google 搜尋候選暫態索引照與 Commons 每日卡片管理者選圖；Commons 中文／英文、授權及容量補充 spike 已完成。
- V3.9.0 桌面與 390×844 UI、可見文案及跨日照片引用規則已核准；前端、Edge Function、Storage migration、領域測試與本機 build 已完成。
- V3.9.0 migration 已在本機 Docker 隔離 Postgres 實際套用；Storage RLS、月額度原子性與瀏覽器角色隔離測試通過，Supabase security／performance advisors 為零問題，Edge Runtime 可載入 `travel-route`。
- V3.9.0 正式 metadata 為一般更新：`minimumSupportedVersion: 3.8.2`、`forceUpdate: false`；遠端隔離 Supabase CI run `34688235879` 已通過且無 annotations。
- V3.9.0 production backend 已完成 logical backup、migration `20260912093527`、production-safe postflight、advisors 與 `travel-route` 部署；兩個新增 action 的未登入請求均為 403。2026-09-13 已將 Commons 縮圖 host 與「換一批」分頁修正部署至 Function version 9（ACTIVE、`verify_jwt=false`），並修正 Supabase Storage 圖片 CSP。
- 正式管理者已完成 Google 候選、Commons 選圖、Storage 保存與重新整理 smoke；每日卡片照片改為只占實際高度，後續說明文字向左回流，作者／授權文字與地圖按鈕上緣對齊且維持原字級及左側間距。桌面與 390×844 驗證通過；重新載入後目前頁面已無 smoke 照片，不自行新增或恢復 production 照片。
- V3.9.0 已於 2026-09-13 正式發布；`main` 與 `v3.9.0` 指向 `7abac83`，GitHub Pages 部署為 `d7b8e93`，正式站顯示 v3.9.0 且 Guest 行程載入正常。
- V3.10.0 使用紀錄仍未定案；原逐次明細、App 查詢頁、獨立角色與 TOTP 方案維持先前草案，不併入 V3.9.0。
- V3.9.1 已於 2026-09-15 開始本機實作。首批完成跨日四碼時間、Maps 地點頁、Commons 每批 6 張與分頁狀態；第二批完成來源可用狀態、候選確認、1:1 手動裁切、1280px 單張安全下載與 640×640 WebP、固定變更聲明，以及候選／已保存封面放大檢視。自動契約、TypeScript、lint 與完整 build 已通過；2026-09-16 已以本機登入管理者完成照片主要互動，並在桌面／390×844 完成跨日四碼時間、IME、刪除、警告焦點與單日／多日複製驗收。
- V3.9.1 Commons 精準搜尋已完成第 14–20 階段的本機接線：Wikimedia fetch transport、核准 Issues 聯絡網址、Edge Function `commonsPrecisionSearch`、AES-GCM 續頁 token、共享快取／配額／鎖／日彙總 migration、公開候選投影與前端搜尋狀態均已實作。隔離 Docker Postgres 已實際重建並通過 RLS、service-role-only、20／分鐘配額、25 秒鎖、快取容量、日彙總原子合併與 retention 驗證；Supabase security／performance advisors 為零問題。自動契約、TypeScript、lint 與完整 production build 均通過。
- 2026-09-16 已補回 V3.0 時期未納入 migration 的 `admin_users`、共用 helper 與資料表前置結構，採獨立 baseline migration、不回寫既有已發布 migration；另新增 forward-only migration 補齊乾淨重建遺漏的 `service_role` 與既有 Data API grants。`npx supabase db reset --local` 已從空白資料庫完整套用所有 migrations，本機管理者登入、Trip 載入與 Edge Runtime 輸入防線均通過。2026-09-17 正式環境只將 baseline history 標記 applied、未重跑其 SQL，再套用三筆後續 migration。
- 2026-09-16 已完成四次核准的 15 例 Wikimedia 真實矩陣（46／64／61／63 次請求，合計 234 次），均完全順序、無重試、無 Supabase／快取／Storage 寫入。第四次確認高千穗峽回傳 6 張候選，且全部有候選案例的 page ID 均無重複，驗證同檔跨證據層去重生效；Product Owner 已核准將桃園第一航廈列為設施級安全停止，`entity-not-found` 不再阻擋精準矩陣閘門。唯一範圍直接搜尋、多實體選擇與設施級停止 UI 已完成本機實作及自動驗證。
- V3.9.1 前端尚未發布；正式 Edge Function 已部署為 version 11／ACTIVE／`verify_jwt=false`。Codex 瀏覽器已完成桌面與 390×844 的候選、裁切、放大、焦點及同名三選項 fixture 回歸。正式 migration、兩項 secrets、production-safe smoke、3.9.1 metadata、完整本機回歸與 release build 均已完成，發布前停止線已達；尚待 `main` 合併、tag、GitHub Pages 與發布後正式站 smoke。Gemini、Pexels／Pixabay 仍未接入，A 自動多語與 C Google Places 脈絡維持停用。
- 排序後全日時間整合、預約紀錄、旅行日誌、保險資訊與 AI 助手維持未排程。

## V3.9.1 停止線前進度計畫

目前已完成的範圍是「本機可重建、後端契約與前端實作」；尚未宣稱可發布。後續依下列順序收斂：

1. **本機互動補驗**：桌面與 390×844 手機的入口、來源、預填、候選選取、確認／裁切、放大、焦點陷阱與焦點返回已通過，桌面儲存亦通過；唯一實體續頁至無 token 終態與真實 `entity-ambiguous` 選項均已通過 Edge／Wikimedia 整合。描述＋QID 的同名判讀介面已完成程式、離線契約及 loopback-only synthetic Edge fixture；Codex 內建瀏覽器已通過三種描述狀態、選定範圍、讀屏名稱與 371×698 窄視窗零水平溢位。既有四輪矩陣與補驗已核准為足夠的真實代表性抽查；selected-QID 真實上游測試降為非阻擋可選 smoke，不再逐地名擴張。
2. **本機整合補驗**：乾淨重建、Trip／角色授權、Edge action、無效 `selectedEntityQid` 修正訊息、續頁 token、快取／配額／鎖、舊表 RLS／policy 及角色矩陣均已通過。
3. **正式環境閘門**：production migration、資料庫 postflight、secrets、Edge version 11 與 production-safe smoke 已完成；沒有執行真實 Wikimedia 查詢或正式資料寫入。
4. **發布準備**：3.9.1 metadata、版本歷史、完整回歸、release build 與本機互動驗收已完成。`main` 合併、tag、GitHub Pages 與發布後 smoke 尚未授權，在完成前不得宣稱 V3.9.1 已發布。

本次修正補入 `supabase/migrations/20260710100000_restore_legacy_schema_prerequisites.sql`，只負責重建歷史上未納入 migration 的本機前置結構；正式環境已有等價物件，故只將該 migration history 標記 applied，未執行其 SQL。其餘三筆 V3.9.1 migration 已正常套用。

## 開發與發布邊界

- 所有新增或調整畫面必須先提供模擬圖，經 Product Owner 確認後才能修改程式。
- 未經 Product Owner 明確要求，不執行正式 Supabase migration、部署、合併、推送或建立 tag。
- 已發布 migration、版本 metadata、版本歷史與 Git tag 不回寫；問題一律以新版本向前修復。
- 修改完成後至少執行與風險相稱的純函式測試、TypeScript、lint、完整 build 與必要實機回歸。

## 尚待處理

- V3.9.1 已以本機合成管理者完成桌面／390×844 的照片設定初始流程；未使用正式資料，也未將合成帳號或 Trip 寫入正式環境。
- V3.9.1 已到達發布前停止線；下一步只有另行授權的正式發布流程：合併 `develop` 至 `main`、建立 `v3.9.1` tag、部署 GitHub Pages 與發布後正式站 smoke。Wikimedia selected-QID 只在出現新風險或上游契約疑慮時另行申請單次授權，不阻擋發布。
- 2026-09-17 正式 migration 已依 preflight 結論完成：baseline `20260710100000` 只 repair history，不執行會回退 helper 安全模式的 SQL；其餘三筆已正常套用。Postflight 確認 migration 完全同步、4 張 Commons 表 RLS、7 個 RPC `SECURITY INVOKER` 且僅 service role 可呼叫、舊表 policy 不再套用 `PUBLIC`、anon 寫入權限為零。Security advisor 的 server-only 表無 policy 資訊屬刻意設計；既有 `tc_delete_trip` 警告已確認函式內仍有 super-admin 驗證，leaked-password protection 未啟用則保留為獨立 Auth 改善項。
- V3.8.2 的 Android／iOS 更新接管、正式管理者多日複製與多裝置衝突列為發布後補驗。
- V3.8.1 的實體裝置、PWA 冷啟動離線及舊版必要更新流程仍待補驗；完整結果見《41_V3.8.1_實作與驗證紀錄》。
- iOS／Android 與兩台實體裝置仍有跨版本發布後補驗證，完整清單見《09_待辦事項_TODO》。
- 離線載入效能、帳本附件管理與其他資料的合併策略只列改善候選，須先量測或取得具體需求，不直接進入開發。
