# 專案現況總覽

> 用途：本文件只記錄目前有效的專案狀態，不保留功能修改過程或已被取代的資訊。
>
> 最後更新：2026-08-23
>
> 目前開發版本：V3.4.5（發布資訊已整理，待手動合併、部署與實機驗證）；已發布版本：V3.4.4。

---

## 已完成功能

- 已完成目前程式碼的 ESLint、TypeScript 與 production build 驗證。
- 初始 JavaScript bundle 已由 1,571.41 kB 降至 489.70 kB；非首屏旅行工具與 Excel 匯出套件採按需載入。
- production PWA 已通過 service worker precache、離線重載與旅行工具切換驗證。
- 實機離線刷新約 30 秒後可正常載入既有本機資料；私人歷史重連／重新載入及私人清單刪除不復活驗證通過。
- Product Owner 已在本機確認 Service Worker 註冊與控制、PWA precache、旅行工具動態載入與切換、Excel 匯出及更新提示顯示。
- V3.4.2 已完成共同清單重連同步、歷史行程離線唯讀、每日行程時間欄位與 PWA Splash 啟動流程的程式及本機驗證。
- V3.4.3 已發布，包含系統 PWA Splash、HTML 預載 Splash 與 React App Splash 的藍色背景銜接及載入動畫。
- V3.4.4 已發布：系統 PWA Splash、HTML 預載 Splash 與 React App Splash 均使用靜態明亮天空藍 `#54B6E7`，並維持 PWA 桌面圖示、網站 favicon、圖示檔與 manifest 圖示設定不變。

### 旅程與導覽

- Trip 建立、編輯、刪除與可編輯者管理。
- 每日行程依活動時間由早到晚顯示；未填時間的活動置底。
- 開啟旅程時，依出發日期自動選取對應 Day；旅程外期間預設 Day 1。
- 已登入使用者可將任一工具設為個人預設首頁；GUEST 不顯示此功能。
- 側欄工具圖示使用海水藍色，並提供預設首頁星號說明。

### 檢查清單

- 共同檢查清單：管理者可新增、編輯、刪除、勾選與排序；項目與分類根目錄皆可排序，並同步雲端。
- 一般登入使用者可自行創建及使用在本機設備上的共同檢查清單。
- 私人確認清單以 `userEmail + tripId` 隔離；已勾選項目自動置底，跨裝置時以最後更新的本機或雲端資料為準。
- 可排序卡片（共同清單、私人清單、領隊／導遊聯絡資訊、其他資訊）支援拖拉與上下箭頭；排序先更新畫面，再延後同步。
- 私人與共同清單的 USER 模式不顯示雲端複製與同步提示。
- 共同／私人清單管理採相同的新增與複製入口；離線時兩者均不可複製，刪除不顯示確認且每筆刪除後暫停刪除操作 1 秒。
- 私人清單依 `tripId + userEmail` 序列化完整雲端同步，pending 保存連續修改的刪除基準並以 revision 安全清除，避免刪除項目或空清單被舊雲端快照復原。
- 登入取得權限後，背景預載今天與未來行程的私人清單；歷史行程只在線上手動進入時載入。
- 共同／私人清單採保守三方合併：不同 ID 聯集，同一 ID 修改優先，刪除與修改衝突時保留修改；雙方修改同一筆時以已同步雲端版本為準，避免跨裝置時鐘誤判。
- 共同清單新增、編輯、刪除與複製在線時立即同步 pending；拖拉與排序採短暫 debounce。
- 共同清單儲存後會立即更新雲端明細；重新進入、App 回到前景或恢復連線時會主動回讀，不使用固定輪詢。

### 參考資訊與旅行工具

- 領隊／導遊聯絡資訊與其他資訊可瀏覽、管理、排序並雲端同步。
- 旅費記帳本支援多人帳本、附件、日期、分攤與 Excel 匯出；混合幣別總覽統一以「幣別代碼＋符號＋金額」的緊密格式顯示支出合計，匯出入口位於帳本明細最下方。
- 新增旅費時，登入 Email 對應名稱作為預設付款人，但可改選其他成員代為記帳；付款人與實際記錄者分開保存。
- 共用帳本以穩定記錄 ID 疊加多人資料，離線待同步帳目依登入者隔離，不以最後登入者整份覆蓋。
- `super_admin` 匯出共用帳本時，會下載目前旅程所有雲端明細，包含其他管理員與 `trip_editor` 建立的紀錄。
- 帳本 XLSX 會以獨立的「支出人幣別合計」工作表，呈現支出人＋幣別的金額合計與明細筆數；原始明細不混入此工作表。
- 帳本 XLSX 先依支出人排序，同一支出人內依幣別明細筆數由多到少排序，筆數相同才以幣別代碼排序。
- 帳本 XLSX 的記帳日期欄固定以完整的 `YYYY-MM-DD` 格式輸出。
- 外幣換算支援換匯紀錄、加權平均、臺銀參考匯率與本機離線參考資料。
- 版本資訊可由所有角色查看；目前版本不會重複出現在版本歷史。
- 手機安裝旅行助手提示於登入及 App 載入完成後顯示；支援 Android 原生安裝事件、iOS／無事件瀏覽器手動說明、7 天稍後冷卻與已安裝模式隱藏。

### 權限與資料

- 角色為 GUEST、USER、trip_editor、super_admin；完整矩陣見 [權限設定資料表](13_權限設定資料表.md)。
- Trip、共同清單、私人清單、其他資訊與帳本已具 Supabase 雲端資料與權限控制；本機資料作為相容與離線備援。

---

## 後續待開發或改善

- V3.4.5 進行 PWA 載入銜接與版本紀錄熱修：網頁層只保留 `#54B6E7` 背景，移除重複圖示／動畫；完成後須以 Android／iOS 已安裝 PWA 驗證無畫面位移感，並確認所有網站與桌面圖示未變動。
- V3.4.4 開發環境與依賴安全基線已完成現況盤點：`.env` 與 Supabase 暫存目錄已正確排除，Node、npm 與 Supabase CLI 版本符合基線。完整 `npm audit` 僅有 ExcelJS 經 `uuid@8.3.2` 的 2 項中度風險，沒有非破壞性修復；不降版 ExcelJS，持續追蹤官方更新。後續依序處理 V3.4.6 Trip 公開／私人與 Guest 存取、V3.4.7 Supabase Storage 附件權限、V3.4.8 Supabase 資料庫權限函式硬化及 V3.4.9 前端瀏覽器安全防護。
- V3.4.6 為單一完整安全發布：既有 Trip 全數轉私人、`is_public`、RLS、REST、靜態網址與離線快取必須在同版 migration 與驗收中完成。已發布版本及已套用 migration 不回寫；問題以新的向前修復版本處理。
- V3.5.0 地點間預估移動資訊保留既有規格，待上述安全版本完成後啟動。
- 離線刷新約需 30 秒才顯示既有本機資料，功能正常但仍可列入後續效能改善。
- V3.4.1 完整規格見 `docs/16_V3.4.1_離線狀態與OtherInfo本機優先同步規格.md`。
- 評估 Supabase advisor 對既有 functions、RLS initplan 與 index 的建議，確認是否需要資料庫調整。
- 改善帳本附件管理體驗。
- 若多人同時修改同一份共同清單或其他資訊，目前以最後成功同步的資料為準；尚未提供衝突合併。
- 暫不規劃新的 Travel Tool（預約、行李、旅行日誌、保險、AI 助手），除非重新立案。
- 後續版本評估將保守聯集分別導入領隊／導遊聯絡資訊、自駕租車資訊、其他／特殊資訊及外幣換算紀錄；各功能依資料性質保留獨立刪除、排序及金額去重規則，不全面共用同一合併結果。

---

## 待驗證事項

- iOS 實機：PWA 安裝、Google 登入、拍照／相簿附件、附件重試、更新提示與同步行為。
- 正式部署後：驗證「馬上更新／稍後更新／我知道了」、版本資訊與 Service Worker 換版行為。
- V3.4.5 發布候選：以 Android／iOS 已安裝 PWA 驗證系統啟動與 App 首畫面的亮藍背景銜接、無網頁 Splash 重複圖示／動畫，以及更新提示內容。
- V3.4.6 發布候選：完成 Guest、`trip_editor`、`super_admin` 的 RLS、REST、直接靜態網址、公開轉私人快取與離線驗收；正式 migration 後全數通過才可標示為已發布。

---

## 維護規則

- 使用者要求「更新文件」、「整理文件」、「準備 Commit」或「要求 Commit」時，必須同步更新本文件。
- 只保留當下有效的功能、待開發／改善與待驗證事項；已完成的驗證須從待驗證移除或移入已完成功能。
- 不在本文件記錄單次修改細節；版本變更過程請保留在 `07_版本更新紀錄.md` 或 Git commit。
