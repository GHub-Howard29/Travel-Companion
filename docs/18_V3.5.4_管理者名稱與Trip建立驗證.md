# V3.5.4 管理者名稱映射與 Trip 建立驗證

> 狀態：發布候選、正式 Supabase migration 與 Super Admin 介面驗收已完成，App 尚未部署
>
> 候選 App metadata 為 V3.5.4；發布日期 2026-08-26；`FORCE_UPDATE = false`

## 實作範圍

- 新增 `admin_profiles`，以 Auth `user_id` 為主鍵，保存最新 Email、固定顯示名稱、`include_in_new_trip` 與 `sort_order`。
- `admin_users` 繼續作為唯一授權來源；`admin_profiles` 不參與角色判斷。
- `admin_profiles` 明確撤銷 `anon` 權限，只向 `authenticated` 授予 CRUD table grants，並以四項 RLS policy 限制為 `super_admin`。
- migration 只為現有兩個 Auth-backed `super_admin` 初始化 profile；`trip_editor` 不建立 profile、不預帶入參與者。既有旅程參與者資料已確認固定映射與排序為 `Howard`（第 1）及 `Carol`（第 2），之後可由 `super_admin` 維護。
- 每次開啟新增旅程表單時在線讀取最新 profile，並依 `include_in_new_trip`、`sort_order` 預帶入 `名稱=Email` 參與者；可編輯者 Email 保持空白。
- 新 Trip ID 固定為 `free-travel-YYYY-MM-DD` 或 `group-tour-YYYY-MM-DD`。既有 Trip ID 與後續編輯行為不變。
- 新增流程先檢查在線狀態、本機／已載入 Trip，再查詢 Supabase；確認無重複後使用 `insert`。雲端成功後才寫入 localStorage，並保留資料庫主鍵作為併發最後防線。

## 修改邊界

- 沒有新增或調整表單版面，僅改變既有欄位的預帶入資料與錯誤處理，因此不需要新的 UI 模擬圖。
- 既有 Trip 更新仍使用 `upsert`；只有「新增旅程」改為不可覆寫的 `insert`。
- 既有 Trip 離線瀏覽、清單／資料／記帳等既有離線能力不變；只有新增 Trip 必須在線。
- 已同步 V3.5.4 App 版本 metadata 與版本歷史；依 Product Owner 指示，本次只 commit／push `develop`，不部署、不合併 `main`、不建立 tag。

## 自動驗證

- `npm run lint`
- `npx tsc --noEmit`
- `npm run build`（包含 release history 與 postbuild 瀏覽器安全檢查）
- `git diff --check`

以上本機檢查已通過。正式 migration `20260826142434` 已套用，`docs/sql/019_v354_admin_profiles_regression.sql` 五項角色／操作回歸全部通過：Super Admin 可見兩列並可維護、一般 User 不可見且不可更新、重複 Trip ID 由主鍵阻擋。

正式資料確認只有 `Howard`、`Carol` 兩列，排序為 0、1，兩列皆預設帶入；`anon` 無 table grant，`authenticated` 具 CRUD grant 並由四項 RLS policy 限制為 `super_admin`。Security advisor 沒有新增 V3.5.4 schema／RLS 警告；既有 Leaked Password Protection 與 checklist 效能建議不屬本版新增項目。

## 正式環境介面驗收

- 以 `haw1971@gmail.com` 的 `super_admin` 身分登入本機候選版並連線正式 Supabase。
- 「新增旅程」視窗可正常開啟，參與者欄精確預帶 `Howard=haw1971@gmail.com`、`Carol=carol1005@gmail.com`，順序正確且沒有第三位管理員。
- 「可編輯者 Email」保持空白，瀏覽器控制台沒有新增錯誤或警告。
- 既有「宜蘭」Trip 的編輯視窗可正常載入名稱、日期與型態；開啟及關閉表單前後仍使用原始 ID `宜蘭-2026-08-29-msw13evb`。程式更新路徑亦沿用既有 `meta.id`／`detail.id`，不依修改後欄位重建 ID。
- 驗收只讀取並關閉表單，沒有建立、修改或刪除正式 Trip。

## 待後續發布作業

- 驗證離線阻擋與本機重複提示；雲端重複及併發主鍵衝突已由正式資料庫回歸覆蓋。
- 部署候選版後驗證正式站版本更新提示，再更新已發布版本基準並完成發布結案。
