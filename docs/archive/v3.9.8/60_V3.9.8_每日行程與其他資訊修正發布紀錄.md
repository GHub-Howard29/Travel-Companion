# V3.9.8 每日行程與其他資訊修正發布紀錄

> 狀態：已發布（2026-09-24）  
> 類型：Patch／一般更新

## 修正範圍

- 統一每日行程有／無封面照片卡片的管理按鈕位置。
- 其他資訊分類捷徑可選擇根分類或子分類，並可開啟既有捷徑。
- 保留說明欄位以單次 Enter 建立的斷行。
- Other Info 本機待同步寫入時保留最後雲端版本時間，避免後續行程儲存誤判為其他裝置更新。

## 已完成驗證

- `npm run verify:v397-itinerary-recovery`、`npm run verify:v391-cover-photo`、TypeScript 檢查、文件連結檢查與正式產物瀏覽器安全檢查通過。
- `npm run build` 已產出 V3.9.8 `dist/app-version.json`。
- Supabase `migration list --linked` 與 `db push --linked --dry-run --skip-vault` 確認正式資料庫同步；本版沒有 migration、Edge Function 或權限部署。
- `main` 合併提交：`9eea1cb`；annotated tag：`v3.9.8`；GitHub Pages 部署提交：`0823a5e`。
- 公開首頁與附不快取查詢的 `app-version.json?release=v3.9.8` 均回應 HTTP 200，版本檔內容為 3.9.8。

## 待補驗

- Android／iOS 實機操作有／無照片行程卡片、其他資訊根／子分類捷徑、單次 Enter 儲存後斷行，以及 Other Info 待同步後的行程儲存。
- 發布前瀏覽器保留既有登入 session，未切換為回歸 fixture，因此上述互動案例不以本次發布前驗證宣告完成。
