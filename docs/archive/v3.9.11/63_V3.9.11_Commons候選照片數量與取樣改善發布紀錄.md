# V3.9.11 Commons 候選照片數量、取樣與選圖體驗改善發布紀錄

> 發布日期：2026-09-25
> 狀態：已正式發布

## 發布內容

- 沿用目前設定照片畫面與既有 UI 排列；已核准文案納入正式版本。
- Commons 候選分為「精準候選」與「需人工確認」兩層；移除 QID 範圍選擇。
- 每批最多 6 張，不足 6 張時隱藏換批；模糊全文搜尋加入 `filetype:bitmap` 限制。
- 修正自行上傳載入生命週期誤報，移除每日行程卡片的照片備註。

## 發布證據

- 發布候選 commit：`7ad0e1f`（已推送 `origin/develop`）。
- `main` 合併提交：`d5c653d`（已推送 `origin/main`）。
- annotated tag：`v3.9.11`（已推送）。
- Supabase：正式專案 `omnnmpcbcicpltkkobae` 的 `travel-route` Edge Function 已部署；後續已套用 backend hotfix migrations `20260925140400_v3911_commons_precision_lock_v2.sql` 與 `20260925142112_v3911_commons_precision_cache_keys_v2.sql`，讓資料庫接受 v2 lock／cache key。
- GitHub Pages：`origin/gh-pages` 部署提交 `0302564`。

## 驗證與 smoke

- `npm run lint`、`npm run verify:v391:all`、`npm run regression:local:verify` 與完整 `npm run build` 通過。
- 完整 build 包含版本歷史、TypeScript、Vite production、PWA 產物及 browser-security／loopback 掃描。
- 桌面與 390×844 候選 UI、廣泛／候選不足 fixture 與單次外部 Wikimedia 矩陣已完成；外部矩陣未重試，修正後結果未宣稱外部複驗通過。
- 正式首頁 HTTP 200、`app-version.json` HTTP 200 且版本為 3.9.11；`travel-route` OPTIONS HTTP 200，未授權輸入邊界 HTTP 400，未產生資料寫入。
- 自行上傳實際選檔驗證依 Product Owner 指示不列入本次發布門檻；已完成程式與靜態契約驗證。
- 正式登入照片搜尋保留由 Product Owner 手動確認，尚未由代理代測。

## 後續補驗

- Android／iOS 實機的候選分層、既有排列與 PWA 更新仍列於 TODO；不影響本次正式發布。
