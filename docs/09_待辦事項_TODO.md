# 待辦事項

> 本文件只保留尚未完成或仍需驗證的工作；版本順序與範圍以《02_產品開發路線圖》為準。
>
> 最後更新：2026-08-24
>
> 已發布版本：V3.4.10；目前待發布版本：V3.4.11；下一個功能版本：V3.5.0。

## V3.4.11 PWA 啟動色彩統一

- [x] 盤點 App 進入後系統資訊列既有色為 `#FFF3E8`，並與 V3.4.1 實際設定逐項比較。
- [x] 原生 Splash 的 `theme_color`、`background_color` 與 HTML 等待底圖統一為 `#FFF3E8`。
- [x] 保留單次原生圖示與等待遮罩，等待階段不新增圖示、動畫或漸層。
- [x] App、公開版本檔、package、V3.4.10 歷史及文件同步為 V3.4.11。
- [ ] Product Owner 合併、建立並推送 `v3.4.11` tag 及部署。
- [ ] Android PWA 從 V3.4.10 更新至 V3.4.11，驗證原生 Splash、等待底圖與 App 系統資訊列的米白銜接。

## V3.5.0 Trip 公開／私人與 Guest 存取

### Migration 與權限

- [ ] 建立 `trips.is_public boolean not null default false` migration，既有 Trip 一次性轉為私人。
- [ ] 確認 Data API grants、Trip RLS 與 `is_public` 變更 trigger；只有 `super_admin` 可切換公開狀態。
- [ ] 讓 Checklist、Other Info 等附屬資料的讀取政策以前置 Trip 權限為準。
- [ ] 以 Guest、`trip_editor`、`super_admin` 回歸 SELECT、INSERT、UPDATE 與 `WITH CHECK`。

### 前端、靜態資料與快取

- [ ] 新建 Trip 預設私人；管理介面只對 `super_admin` 顯示公開設定。
- [ ] 私人轉公開前顯示個資及不可收回既有下載資料的警告。
- [ ] 公開與私人 Trip 採不同授權載入流程，移除 `public/trips/` 靜態資料。
- [ ] 公開快取與每個 `userId` 的私人快取分離；登出或失去權限後不得讀取私人快取。
- [ ] 公開轉私人後，在下一次成功刷新時清除未登入者及失去權限帳號的舊快取。

### 發布與驗收

- [ ] 在隔離環境演練 migration，建立正式資料備份、筆數與 ID 清單。
- [ ] 候選前端在資料庫尚未升級時維持安全唯讀，不提供失敗或不安全的公開設定操作。
- [ ] 同一維護窗口部署候選前端、確認舊靜態網址為 `404`、套用 migration 並立即驗收。
- [ ] 全部通過後才標記 V3.5.0 已發布；失敗時只使用向前修復 migration／新版程式。
- [ ] 發布前最終確認 `FORCE_UPDATE`；目前建議為 `true`。

## 後續版本

- [ ] V3.5.1：Supabase Storage 附件權限。
- [ ] V3.5.2：Supabase 資料庫權限函式硬化。
- [ ] V3.5.3：前端瀏覽器安全防護；開始前重新確認範圍與版號。
- [ ] V3.6.0：地點間預估移動資訊，依專屬規格開發。

## 跨版本待驗證／改善

- [ ] iOS：PWA 安裝、Google 登入、拍照／相簿附件、附件重試、更新提示與同步。
- [ ] 評估離線刷新約 30 秒才載入既有資料的效能改善。
- [ ] 評估帳本附件管理體驗。
- [ ] 依資料特性評估將保守聯集合併導入其他資訊及外幣換算，不共用單一合併策略。
