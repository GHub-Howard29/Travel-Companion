# V3.5.3 前端瀏覽器安全防護驗證

> 建立日期：2026-08-26
>
> 狀態：發布準備完成，待 Product Owner 合併與部署

## 一、修改邊界

- 不修改 Supabase schema、migration、資料列權限（RLS）或角色矩陣。
- 不修改 Trip、Offline First、同步佇列、附件儲存或 Excel 匯出資料流。
- 不修改 GitHub Pages base path、PWA scope、start URL 或 Service Worker 更新策略。
- 僅處理瀏覽器安全邊界、正式 build 的安全政策與必要回歸。

## 二、安全修正

- 正式 build 加入 Content Security Policy（CSP，內容安全政策）；開發模式不強制 CSP，避免阻擋 Vite 開發連線。
- `script-src` 僅允許同源程式，不開放 `unsafe-inline` 或 `unsafe-eval`。
- Supabase API 與 Realtime 僅依 `VITE_SUPABASE_URL` 產生 HTTPS／WebSocket 連線來源。
- 保留 PWA、Service Worker、附件與 App 圖片所需的同源、`blob:` 與 `data:` 範圍。
- Referrer Policy 設為 `no-referrer`。
- iOS standalone PWA 啟動偵測改為同源外部程式，維持 React 啟動前執行。
- OAuth 等待畫面移除 `innerHTML`，改用 DOM API 與 `textContent`。
- OAuth 導向網址限制為目前設定的 Supabase origin。
- 地圖、文字連結與附件視窗統一驗證 HTTP／HTTPS 並隔離 `window.opener`。
- 使用者內容中的 URL 會拒絕非 HTTP／HTTPS、無效網址及含帳號密碼的網址。

## 三、GitHub Pages 限制

- CSP meta 無法使用 Report-Only、`frame-ancestors` 或回報端點。
- Permissions Policy 必須由 HTTP response header 提供；本專案目前的 GitHub Pages 部署無自訂 header 設定，因此本版不加入無效 meta。
- `frame-src 'none'` 僅禁止 App 載入子 frame，不能取代 `frame-ancestors` 的防嵌入能力。

## 四、驗證矩陣

| 項目 | 自動驗證 | 實機／正式角色驗證 | 狀態 |
| --- | --- | --- | --- |
| TypeScript、ESLint、正式 build | `npm run lint`、`npm run build` | 不需要 | 自動驗證通過 |
| CSP 與 Referrer Policy | postbuild 安全檢查、正式 HTML 回應 | 瀏覽器 Console | 自動與測試瀏覽器 Console 驗證通過 |
| Supabase REST 與 Realtime | CSP connect source 與正式 build | 登入後跨裝置刷新 | 實機與正式角色驗證通過 |
| 一般瀏覽器 Google OAuth | 基本頁面與登入入口 | 完成 Google 登入 | 實機驗證通過 |
| iOS standalone PWA OAuth | 啟動程式與安全視窗程式檢查 | iOS 主畫面 App 登入 | 延後補驗證（不列為本次發布阻擋項目） |
| PWA／Service Worker | manifest、SW 產生與 precache build | 安裝、離線重開、恢復連線 | 實機驗證通過；正式更新提示待部署後驗證 |
| 雲端簽名附件 | URL、協定與路徑檢查 | 所屬 Trip 角色開啟附件 | 實機驗證通過 |
| 本機 blob 附件 | CSP `blob:` 與同步開窗 | 離線附件開啟 | 實機驗證通過 |
| Excel 匯出 | chunk build | 檔案選擇器、下載備援與 CSP Console | 實機驗證通過 |
| 分享、地圖、其他資訊連結 | URL 與 opener 隔離檢查 | 行動裝置原生分享 | 實機驗證通過 |

## 五、目前驗證結果

- 修改前基準：`npm run lint`、`npm run build` 通過。
- 修改後：`npm run lint`、版本歷史、TypeScript、Vite PWA build 與 postbuild 安全檢查通過。
- 正式 HTML HTTP 回應已確認包含 CSP、`no-referrer`、同源 iOS 啟動程式、Supabase HTTPS 與 Realtime WebSocket 來源。
- 隔離連接埠的測試瀏覽器已確認 CSP、Referrer Policy、同源 iOS 啟動程式與載入 Console 無錯誤；登入後互動則以實際帳號與裝置完成回歸。
- 一般瀏覽器 Google OAuth、正式角色權限、跨裝置 Realtime、雲端簽名附件及本機 `blob:` 附件已完成實機驗證。
- 跨裝置刪除同步約 5–10 秒，自動更新，無須刷新；列為通過，後續持續觀察延遲。
- Excel 匯出已完成實機驗證：可下載、開啟兩個工作表、帳目／日期／金額與附件連結正確，管理者可匯出目前 Trip 全部共用明細。
- 地圖導航、其他資訊 HTTPS 連結與 App 分享已完成實機驗證：外部目的地另開分頁，原 App 保留；分享連結可回到 App 首頁。
- PWA 已完成實機驗證：可安裝、重開保留既有資料與登入狀態、離線重開可載入快取內容、恢復連線後可正常使用。新版本更新提示須待部署 V3.5.3 後另行驗證。
- iOS standalone PWA Google OAuth 因目前沒有可供手機存取的 HTTPS 測試站而延後補驗證；不列為本次發布阻擋項目，驗收條件移至待辦清單。

## 六、發布原則

- V3.5.3 已更新 App 版本 metadata，候選版本為 `3.5.3`，採非強制更新。
- 本階段完成開發、驗證、版本 metadata、Commit 與 push；由 Product Owner 接手合併主分支、建立 tag 與部署正式站。
