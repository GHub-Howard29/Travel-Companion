# V3.6.0 發布前測試與正式發布流程

> 狀態：V3.6.0 已於 2026-08-30 合併至 `main`、建立 Git 版本標籤 `v3.6.0` 並部署 GitHub Pages；正式後端、Google Cloud 與桌面發布驗證完成
>
> 建立日期：2026-08-28
>
> 繼續流程觸發語：`3.6.0下一階段`
>
> 2026-08-29 發布決策：目前沒有 staging，Product Owner 決定採正式環境受控發布。正式後端就緒後、前端發布前完成電腦版整合測試；Android／iOS 手機實機測試改列發布後補驗證，不得記為發布前通過。

---

## 一、用途與恢復規則

本文件是 V3.6.0 後續測試、正式資料庫變更與發布順序的操作依據。

Product Owner 指出「3.6.0下一階段」後，Codex 必須：

1. 先讀取本文件、《15_V3.6.0_地點間預估移動資訊規格》與《09_待辦事項_TODO》。
2. 檢查 Git 工作目錄、目前分支、已發布版本、Supabase CLI／專案連結及尚未完成項目。
3. 從本文件第一個未完成階段開始，不重做已完成且有證據的項目。
4. 一次只引導一個安全階段，完成檢查並回報結果後再進下一階段。
5. 正式 migration、正式 Edge Function 部署、正式前端發布、Git 版本標籤等外部變更，均須在執行前指出目標與影響並取得 Product Owner 當次明確同意。
6. 不使用 `db reset --linked`、不在正式環境加入 seed，也不直接以 Dashboard 改寫正式 schema 或 migration history。

---

## 二、目前交接狀態

已完成：

- V3.6.0 地點確認、交通區段、路線方式、時間警告與外部 Google Maps 操作介面。
- 過期估算清除、永久交通方式偏好、離線資料相容與「路線資訊待更新」顯示。
- `travel-route` Edge Function、每日 100 次專案配額、30 天快取、權限檢查及輸入驗證。
- migration `20260828085602_v360_route_estimate_cache_and_quota.sql` 與 `020` 安全驗證 SQL。
- ESLint、TypeScript、行程交通測試、Edge Function 純函式測試、production build 與瀏覽器安全檢查。
- 正式 Supabase migration `20260828085602` 已套用，`020`、advisors、migration history 與 dry-run 均已驗證。
- 正式 `travel-route` Edge Function 已部署且保持 JWT 驗證；`GOOGLE_MAPS_API_KEY` 只存在 Function secret。
- Google Cloud 的 Places API (New)、Routes API、同一把 Server Key 的兩項 API 限制、初始配額與每月 US$5 預算警示已由正式 Console 核對。
- `http://localhost:4173/Travel-Companion/` 已以 `haw1971@gmail.com`／super_admin 完成 V3.6.0、正式 Supabase 資料載入與 Routes 查詢驗證；瀏覽器只呼叫 Supabase Edge Function，未直接呼叫 Google API。
- 正式 merge commit `0cce637`、帶註解 Git 版本標籤 `v3.6.0` 與 gh-pages commit `f16e0dca` 已推送；正式 metadata 與首頁資源均為 V3.6.0。
- 關閉舊 V3.5.6 分頁後，新 Service Worker 已接管；正式站顯示 V3.6.0、Guest 行程與已存路線正常。

尚未完成：

- 補齊所屬 trip_editor、一般 User 與其他 Trip editor 的完整角色矩陣；目前已有 super_admin 成功查詢與未登入請求 401 證據。
- 正式站 Google OAuth 本次自動化點擊未完成跳轉；既有 OAuth 設定未修改，發布前 `localhost:4173` 登入驗證已通過。
- Android／iOS 手機實機測試改列發布後補驗證。

---

## 三、階段 A：恢復與唯讀盤點

- [x] 確認工作目錄的修改均屬 V3.6.0 程式、版本 metadata 或發布文件。
- [x] 發布前確認分支為 `develop`、基準版本為 V3.5.6；發布後最新正式版本為 V3.6.0。
- [x] 重新執行 `npm run lint` 與 `npm run build`。
- [ ] 檢查 Supabase 與 Google Maps 現行 changelog、API、快取、attribution 與費用規範是否有影響本版的變更。
- [x] 確認 Supabase CLI 為 2.115.0，並以 `--help` 核對當次指令參數；2.116.0 更新不在發布中途套用。
- [x] 明確確認正式 Supabase project ref 為 `omnnmpcbcicpltkkobae`、Google Cloud project ID 為 `keen-enigma-483806-i9`。
- [x] 確認 `GOOGLE_MAPS_API_KEY` 只存在 Supabase Function secret，不出現在 PWA、Git diff、log 或 `VITE_` 環境變數。

通過條件：程式仍可建置、沒有未知修改、目標環境與金鑰邊界明確。

---

## 四、階段 B：開發／staging 整合測試

> 2026-08-29 決策：目前沒有 staging，本階段不以正式環境任意重現配額上限或破壞性失敗情境。可安全完成的檢查併入階段 D：先完成 migration history／dry-run、備份與發布時段確認；取得當次同意後依正式順序部署後端，先以電腦完成必要整合與角色 smoke test，通過後才發布前端。手機實機項目保留為發布後補驗證。

### 4.1 資料庫與 Edge Function

- [ ] 先執行 migration list，確認 staging migration history 沒有分歧。
- [ ] 執行 migration dry-run，只允許預期的 V3.6.0 migration。
- [ ] 對 staging 套用 migration，不包含 seed。
- [ ] 執行 `docs/sql/020_v360_route_estimate_security_validation.sql`。
- [ ] 執行 Database Security Advisor 與 Performance Advisor；不得留下與本版新增物件有關的高風險項目。
- [ ] 設定 staging `GOOGLE_MAPS_API_KEY` secret，部署 `travel-route` Edge Function。
- [ ] 確認 Function 保持 JWT 驗證，不使用 `--no-verify-jwt` 部署正式設定。

### 4.2 角色與費用邊界

| 測試身分 | Places 搜尋 | Routes 查詢 | 查看已存結果 | 點擊外部 Google Maps |
|---|---:|---:|---:|---:|
| Guest | 必須拒絕 | 必須拒絕 | 可以 | 可以，且不呼叫專案 API |
| User | 必須拒絕 | 必須拒絕 | 可以 | 可以，且不呼叫專案 API |
| 其他 Trip 的 trip_editor | 必須拒絕 | 必須拒絕 | 可以 | 可以，且不呼叫專案 API |
| 所屬 Trip 的 trip_editor | 可以 | 可以 | 可以 | 可以 |
| super_admin | 可以 | 可以 | 可以 | 可以 |

- [ ] 未登入請求由 Supabase gateway 拒絕，不能到達付費 Google API。
- [ ] 一般 User 與其他 Trip editor 由 Function 角色檢查拒絕。
- [ ] 只有所屬 Trip editor 與 super_admin 能產生 Places／Routes 查詢。
- [x] API key、service role／secret key 不出現在瀏覽器 Network response 或 bundle。

### 4.3 地點與卡片 UI

- [ ] 原卡片維持既有資訊結構，不新增景點圖案或地址列。
- [ ] 地點按鈕顯示「在地圖中查看」，開啟地點頁而非誤稱導航。
- [ ] 地點候選只在確認面板暫時顯示，Trip 長期只保存 Place ID 與原 `location` 文案。
- [ ] 次要按鈕顯示「保留原設定地點」。
- [ ] 修改地點文字會解除舊 Place ID 與舊估算，但保留最後交通方式偏好。
- [ ] Google Maps attribution 顯示符合當下官方規範。

### 4.4 交通區段與時間規則

- [ ] 兩張直接相鄰且 Place ID 不同的卡片才建立交通區段。
- [ ] 中間有無地點卡片時不得跨卡計算。
- [ ] 相同 Place ID 不建立區段。
- [ ] 明確標記的機場到機場航班不建立地面交通區段。
- [ ] 開車、步行、大眾運輸均可查詢、預覽與儲存。
- [ ] 管理者只開啟交通面板時不查詢；明確點選交通方式才呼叫 Routes API。
- [ ] Guest／User 點擊已存或待更新區段只開啟 Google Maps，不呼叫專案 Routes API。
- [ ] 大眾運輸使用前一站有效離開時間；無班次時日間備援文案正確。
- [ ] 起終點、交通方式或大眾運輸離開時間改變時，舊估算正確失效。
- [ ] 只修改標題／說明不讓估算失效；下一站到達時間只重算警告。
- [ ] 時間衝突優先於時間不足，兩者皆不自動修改行程時間。

### 4.5 快取、過期與配額

- [ ] 相同快取鍵重複查詢時使用 cache，不增加 Google Routes 外部查詢。
- [ ] cache miss 才宣告每日配額並呼叫 Google Routes。
- [ ] 過期估算不再顯示時間／距離，但保留交通方式及「路線資訊待更新」。
- [ ] 雲端讀寫與純離線本機讀取都會清除過期估算值。
- [ ] 每日 100 次上限以 staging 測試資料或受控方式驗證，不為測試實際送出 100 次付費 Google 請求。
- [ ] 達上限後既有結果仍可讀取，新 cache miss 回傳可辨識錯誤。

### 4.6 PWA 與裝置回歸

- [ ] 桌面瀏覽器、Android PWA 與可取得的 iOS 裝置檢查卡片間距、面板與外部開圖。
- [ ] 線上儲存後重新整理、切換 Day、切換 Trip 不會自行查詢 Routes。
- [ ] 離線仍可讀取有效的已存結果；過期結果顯示待更新，不嘗試背景查詢。
- [ ] 重新連線不覆蓋尚未同步的行程變更。

通過條件：上述必要項目全部通過；若設備無法取得，必須記錄為已知未驗證，不得寫成通過。

---

## 五、階段 C：建立 V3.6.0 候選版本

此階段須先由 Product Owner 確認測試結果及發布內容。

- [x] 更新 App、`package.json`、版本 metadata 與版本歷史為 `3.6.0`。
- [x] `minimumSupportedVersion` 設為 `3.5.6`、`forceUpdate = true`；V3.5.6 可選擇稍後更新，更舊版本必須更新。
- [x] 更新 App 內版本資訊、文件與必要更新測試。
- [x] 再次執行完整 lint、build、PWA、安全及 V3.6.0 測試。
- [x] 候選程式 commit：`3e89cbc`；lint、production build、PWA/postbuild、安全檢查與密鑰掃描均通過。正式前端與 Service Worker 已發布驗證；剩餘風險為完整角色矩陣、正式站 OAuth 與 Android／iOS 實機驗證。

通過條件：候選版本內容鎖定且所有發布阻擋項目清零。

---

## 六、階段 D：正式環境發布順序

本階段每一項外部變更都要先回報目標正式專案並取得 Product Owner 當次同意。

### 6.1 正式 migration 前

1. [x] 確認正式 Supabase project ref 為 `omnnmpcbcicpltkkobae`，不依賴先前連結狀態。
2. [x] roles、schema、data 三份診斷備份與總驗證已完成；發布採受控時段。
3. [x] 執行正式 migration list，local／remote history 完全一致。
4. [x] 執行 `db push --dry-run --skip-vault`，結果為 `Remote database is up to date.`，沒有待套用 migration／seed／roles。
5. [x] 未出現 history 分歧，未執行 `migration repair`。

### 6.2 正式發布

1. [x] 正式 migration `20260828085602_v360_route_estimate_cache_and_quota.sql` 已套用，不包含 seed。
2. [x] `020`、Security Advisor 與 Performance Advisor 已驗證；沒有 V3.6.0 新增物件警告，既有 Auth 密碼外洩保護與 checklist RLS 效能警告保留追蹤。
3. [x] V3.6.0 的 RLS、grants、函式、PK 與索引全部通過。
4. [x] 正式 `GOOGLE_MAPS_API_KEY` secret 已存在；Google Cloud Server Key 僅限制 Places API (New) 與 Routes API。
5. [x] `travel-route` Edge Function 已部署並保持 `verify_jwt = true`。
6. [ ] 以所屬 trip_editor／super_admin 各做最少量成功 smoke test；以 Guest／User／其他 Trip editor 驗證拒絕與不產生付費查詢。
7. [ ] 以前端候選內容在電腦瀏覽器完成 Places、三種 Routes、儲存、快取、時間警告、權限與金鑰不洩漏整合測試。
8. [x] 資料庫、Function 與電腦版整合測試正常後，已發布 V3.6.0 前端。
9. [x] 正式 metadata、Service Worker 接管、版本顯示、主要 Guest 行程及已存路線已通過；正式站 OAuth 另列發布後補驗證。
10. [ ] 發布後補驗證 Android／iOS 手機的版面、PWA、外部開圖、離線與重新連線；無設備項目記為未驗證，不得記為通過。

### 6.3 2026-08-30 已完成驗證紀錄

- Google Cloud 帳號：`haw1971@gmail.com`；專案：`Travel Companion Maps Prod`（`keen-enigma-483806-i9`）。
- Places API (New)：已啟用；`AutocompletePlacesRequest` 初始配額為每日 50、每分鐘 10。
- Routes API：已啟用；`Directions - ComputeRoutes` 初始配額為每日 50、每分鐘 10；未使用的 Route Matrix 配額維持 0。
- 金鑰：`Travel Companion Maps Prod Server Key`，API 限制只包含 Places API (New) 與 Routes API。
- 預算：`Travel Companion Maps Prod monthly guardrail`，每月 US$5，50%／90%／100% 警示，核對時支出為 US$0。
- migration：local／remote history 一致；正式庫 dry-run 回報 up to date。
- `020`：兩張 V3.6.0 table 均啟用 RLS，僅 `postgres`／`service_role` 有 table grants；`tc_claim_route_query_slot()` 為 security invoker、空 `search_path`，只有 `service_role` 可執行；PK 與過期索引存在。
- Advisors：沒有 V3.6.0 新增物件警告；既有警告不由本版擴大。
- Function：ID `1da63c50-50cf-4815-827a-c867de1f193d`，version 1，2026-08-30 09:36:44（Asia/Taipei）部署，狀態 ACTIVE、JWT 驗證開啟。
- Function logs：可見已登入請求 `POST 200`，以及未登入請求由 gateway 以 `POST 401` 拒絕；未以測試重現每日上限。
- 桌面整合：只認列 `http://localhost:4173/Travel-Companion/`。V3.6.0 以 `haw1971@gmail.com` 登入並載入正式 Supabase 行程與 super_admin 權限；不儲存地切換步行得到約 2 小時 29 分鐘／11 公里，切回開車得到約 21 分鐘／13 公里。
- 瀏覽器 Network：實際路線請求為 `https://omnnmpcbcicpltkkobae.supabase.co/functions/v1/travel-route`；沒有瀏覽器直連 Places／Routes Google API。production bundle 與瀏覽器資源未找到 Google key、`GOOGLE_MAPS_API_KEY`、`X-Goog-Api-Key` 或 Google Maps Web Service endpoint。
- 本次只切換預覽交通方式並關閉面板，沒有按「儲存這個交通方式」，正式行程資料未變更。
- 正式發布：main merge commit `0cce637e21eaca2943ee9afb2f579b426edf0642`；帶註解 Git 版本標籤 `v3.6.0`；gh-pages commit `f16e0dca5757a3edbf174a352616f6e0adc71443`。
- 正式站：`app-version.json` 回傳 V3.6.0；首頁引用 `index-C-tVtLX0.js`／`index-DAAl7DED.css`；關閉舊分頁後新版 Service Worker 接管並顯示 V3.6.0、Guest 行程及已存路線。
- 正式 OAuth：自動化點擊未完成 Google 跳轉，未標示通過；既有 OAuth 設定未修改，保留發布後人工／實機補驗證。

正式順序固定為：

```text
正式 migration
→ SQL／Advisors 驗證
→ Edge Function
→ 正式 smoke test
→ V3.6.0 前端
→ 發布後驗證與文件
```

---

## 七、停止與回復原則

- migration 或驗證失敗：停止後續部署，保存完整錯誤；不得反覆重跑或直接 repair history。
- migration 已成功、Edge Function 尚未成功：不發布前端；修正 Function 後再部署。
- Edge Function 已成功、前端尚未發布：舊版 v3.5.6 不會呼叫新 Function，可安全停在此階段排查。
- 前端發布後發現問題：優先回退／停用前端新入口；本次 additive schema 不因一般發布回退而刪除。
- 已套用正式 migration 視為不可回寫；需要資料庫修正時建立新的向前修復 migration。
- 不使用正式備份作為一般版本回退工具；備份只供災難復原。

---

## 八、發布完成紀錄

全部完成後更新：

- `docs/07_版本更新紀錄.md`
- `docs/09_待辦事項_TODO.md`
- `docs/14_專案現況總覽.md`
- `docs/15_V3.6.0_地點間預估移動資訊規格.md`
- `docs/21_V3.6.0_發布前測試與正式發布流程.md`
- App 版本歷史與更新政策檔案

並記錄：

- migration timestamp 與遠端 history 結果
- Edge Function deployment ID／時間
- `020`、Security Advisor、Performance Advisor 結果
- 四角色回歸與 Google API 實際查詢結果
- 發布 commit、Git 版本標籤、部署時間與已知未驗證項目

---

## 九、暫停點

V3.6.0 正式發布已完成，發布文件回寫於 `develop`。後續只追蹤完整角色矩陣、正式站 OAuth 與 Android／iOS 實機補驗證；不得將未執行項目補記為通過。
