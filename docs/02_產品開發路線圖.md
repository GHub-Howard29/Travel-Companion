# 產品開發路線圖

> 本文件只管理未來版本的編號、順序與範圍。目前正式版本、部署／發布狀態與目前開發目標，唯一以《[14_專案現況總覽](14_專案現況總覽.md)》為準。
>
> 最後整理：2026-09-24

## 版本編號原則

- `MAJOR`：產品世代、核心架構或資料模型有大規模且不相容的變更。
- `MINOR`：新增使用者可感知的主要功能，或進入新的產品功能階段。
- `PATCH`：Bug 修正、小型 UI／體驗改善、效能調整、安全維護或既有功能補強。
- `forceUpdate` 與版本層級分開判斷；資料不相容、安全、RLS／schema、同步或舊快取有重大風險時才建議必要更新。
- 版本發布後不回寫版號、migration 或版本歷史；問題以新版本向前修復。

## 已發布版本索引

本節只提供導覽，不是目前版本狀態的權威來源。完整發布帳冊見《[07_版本更新紀錄](archive/07_版本更新紀錄.md)》。

| 版本範圍 | 歷史文件 |
|---|---|
| V3.4.1～V3.5.6 | [archive/v3.4-v3.5/](archive/v3.4-v3.5/) |
| V3.6.0～V3.6.5 | [archive/v3.6/](archive/v3.6/) |
| V3.7.0～V3.7.4 | [archive/v3.7/](archive/v3.7/) |
| V3.8.0～V3.8.2 | [archive/v3.8/](archive/v3.8/) |
| V3.9.0～V3.9.5 | [archive/v3.9.0-v3.9.5/](archive/v3.9.0-v3.9.5/) |
| V3.9.6 | [archive/v3.9.6/](archive/v3.9.6/) |
| V3.9.7 | [archive/v3.9.7/](archive/v3.9.7/) |
| V3.9.8 | [archive/v3.9.8/](archive/v3.9.8/) |
| V3.9.9 | [archive/v3.9.9/](archive/v3.9.9/) |

## 排程中版本

### V3.9.10 每日行程卡片資訊與操作列精簡

- 移除卡片內重複的照片作者／授權註解，保留照片下方的來源原處連結與照片檢視器完整資訊。
- 其他資訊捷徑與「查看地圖」維持緊鄰並整組靠右，「查看地圖」固定在最右側。
- 保留「查看地圖」左側定位圖示，移除右側重複的聯外圖示。
- 不包含 migration、Edge Function、RLS、Storage 或正式資料內容變更。

### V3.9.11 驗證套件整併與建置效能基線

- 建立完整 build 中各驗證群組、TypeScript 與 Vite build 的耗時基線。
- 依領域整併驗證入口與失敗報告，但不縮減 release build 的完整發布驗證。
- V3.9.3 已在 `src/main.tsx`／`src/App.tsx` 建立 `tc_app_bootstrap_start` 與 `tc_app_bootstrap_duration` 總啟動量測；V3.9.11 不重複新增程式內量測埋點，僅量測驗證與建置流程耗時。
- 不包含功能、資料庫、Edge Function、正式環境、版本 metadata 或部署變更。

### V3.9.12 離線冷啟動剖析與定向改善

- 沿用 V3.9.3 既有的總啟動量測，在本機新增 Service Worker／navigation、session、Trip 快取、localStorage、IndexedDB 與首個可操作畫面的分段耗時量測，以找出離線冷啟動約 30 秒的瓶頸。
- 只在證據足夠時實作定向改善；不破壞 Offline First、墓碑、pending 與同步安全。
- 不重複 V3.6.5 的更新後載入改善或 V3.9.3 的冷啟動總埋點；量測資料維持本機 Console，不新增遠端 telemetry 或蒐集行程、帳目、Email 等資料。

### V3.9.13 零費用 Playwright 響應式 E2E 回歸基礎

- 只使用 loopback fixture 與本機瀏覽器／viewport 模擬，不使用 production、外部 API 或付費雲端實機。
- 優先覆蓋核心導覽、窄螢幕溢位、重要對話框與既有合成資料契約。
- 不以模擬取代 Android／iOS 實體裝置的 PWA、OAuth、相機、離線與跨裝置驗證。

### V3.9.14 照片功能模組化、型別邊界與按需載入

- 沿用既有 lazy loading 與 Excel 動態載入成果，不重複開發。
- 以現有照片搜尋、裁切、上傳與來源 metadata 的型別邊界為主，不擴張新圖庫來源。

## 尚未定案

### V3.11.0 使用紀錄重新評估

- V3.9.3 已完成唯一系統開發者 capability 與最小彙總使用紀錄。
- 逐次明細、App 查詢頁、獨立角色、Cron 與 TOTP 仍是先前方案草案，不納入近期開發。
- 需求若重啟，必須重新確認必要性、最小資料範圍、保存期、權限與復原機制。

## 暫不排程

- 排序後的全日時間整合、預約紀錄、旅行日誌、保險資訊與 AI 助手。
- Google Maps 任意落針座標要作為交通估算點時，需獨立確認資料契約、UI、隱私、成本與 Google 條款。
