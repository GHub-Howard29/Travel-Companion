# 產品開發路線圖

> 本文件是未來版本編號、順序與範圍的唯一權威來源。
>
> 最後更新：2026-09-01
>
> 已發布 App 版本：V3.6.2；Git 版本標籤 `v3.6.2` 與手動部署已完成。

## 版本編號規則

採 `MAJOR.MINOR.PATCH`：

- `X`（Major）：產品世代、核心架構或資料模型發生大規模且不相容的改變。
- `Y`（Minor）：新增一項明確、可被使用者感知的主要功能，或進入新的產品功能階段。
- `Z`（Patch）：Bug 修正、小型 UI／體驗改善、效能調整、安全維護或既有功能補強。
- `FORCE_UPDATE` 與版本層級分開判斷；只有資料不相容、安全、RLS／schema、同步或舊快取有重大風險時才建議強制更新。

版本發布後不可回寫版號、migration 或版本歷史；若發現問題，使用新的向前修復版本。

## 已發布基線

### V3.4.6 PWA 啟動銜接與版本資訊優化（已發布）

- 已完成合併、部署及發布。
- 改善 Android 瀏覽器／PWA 系統列、啟動背景與 App 內容底色的銜接。
- 版本資訊主畫面顯示目前版本與最近兩個舊版本，其餘由完整版本歷史查看。
- 不包含 Supabase schema、RLS、migration 或 Trip 隱私功能。

### V3.4.7 V3.4.6 發布資訊補正（已發布）

- V3.4.6 已完成程式合併、Git 版本標籤與部署，但發布 metadata 遺漏升版，導致 App 與網頁仍顯示 V3.4.5。
- 以新的 Patch 版本向前補正為 V3.4.7，不刪除、不移動也不覆寫既有 Git 版本標籤 `v3.4.6`。
- App、公開版本檔、package 與版本歷史同步為 V3.4.7；`PREVIOUS_RELEASE_VERSION` 指向 V3.4.6。
- 本版不包含功能、資料庫、RLS、migration 或 Trip 隱私變更，`FORCE_UPDATE = false`。
- 已完成合併、建立 Git 版本標籤 `v3.4.7` 與部署。

### V3.4.8 PWA 更新重載流程修正（已發布）

- 問題原因：按下「馬上更新」後，程式在新版 Service Worker 完成頁面接管前先重新載入，接管事件又可能觸發第二次重載；因此可能再次載入舊版並重複顯示更新提示，Android PWA 也可能停留在純色啟動畫面。
- 修正更新流程，只在新版 Service Worker 的 `controlling` 事件發生後重新載入頁面。
- 加入單次重載與更新中防重入，避免接管事件、備援流程或重複點擊造成多次重載。
- Android WebView 保留延遲備援重載，但不再與正常接管流程競速。
- 本版不包含資料庫、RLS、migration 或 Trip 隱私變更，`FORCE_UPDATE = false`。
- 已由 Product Owner 完成合併、建立 Git 版本標籤 `v3.4.8` 與部署。
- V3.4.7 升級當下仍由舊版更新程式執行，實機曾停留純色畫面一次；完全關閉後可正常進入 V3.4.8。新版防重載流程留待 V3.4.8 升級至下一版時驗證。

### V3.4.9 清單同步與 PWA 載入銜接修正（已發布）

- 共同清單初始雲端同步不得以舊快照覆蓋同步期間發生的新勾選操作。
- 共同清單連續勾選依序寫入雲端，避免網路回應順序顛倒造成下次載入狀態錯誤。
- 只有已結束的歷史行程在離線時顯示完整唯讀提示；尚未結束及未來行程維持原有本機操作與同步規則。
- HTML／React 載入期間持續使用一致的藍底 PWA 圖示畫面，首個功能頁完成後才切入操作介面。
- 本版不包含資料庫、RLS、migration 或 Trip 隱私變更，`FORCE_UPDATE = false`。
- Product Owner 已完成合併至 `main`、建立 Git 版本標籤 `v3.4.9` 與部署。
- Android PWA 的升級流程、首次共同清單操作與歷史行程離線提示已通過；載入畫面問題由 V3.4.10 修正。

### V3.4.10 PWA 重複啟動圖示修正（已發布）

- Android 原生 Splash 保留 PWA 圖示，不變更 manifest、桌面圖示與 favicon。
- 網頁載入階段移除重複 App 圖示，只保留一致藍底遮罩，首個功能頁完成後才切入操作介面。
- 本版不包含資料庫、RLS、migration 或 Trip 隱私變更，`FORCE_UPDATE = false`。
- Product Owner 已完成合併、建立 Git 版本標籤 `v3.4.10` 與部署；實機確認第二張網頁層圖示已移除。
- 實機仍可看到原生 Splash 上方系統分隔陰影與下方白色導覽列，色彩銜接由 V3.4.11 改善。

### V3.4.11 PWA 啟動色彩統一（已發布）

- Android 原生 Splash 的 `theme_color` 與 `background_color` 統一為 App 系統資訊列既有色 `#FFF3E8`。
- HTML 等待遮罩、文件與根節點載入底色同步改為 `#FFF3E8`，完成後仍一次切換至操作介面。
- 保留 V3.4.10 的單次原生圖示流程，不新增網頁層圖示、動畫或漸層。
- HyperOS 原生陰影與三鍵導覽列仍由作業系統決定，本版目標為降低色差，不保證移除系統效果。
- 本版不包含資料庫、RLS、migration 或 Trip 隱私變更，`FORCE_UPDATE = false`。
- Product Owner 已完成合併、建立 Git 版本標籤 `v3.4.11`、部署及 Android PWA 實機驗證。
- 色彩與轉場功能驗證通過；實際視覺效果尚未完全符合 Product Owner 期待，但本階段不再追加修正，先行結案。

## 核定 Roadmap

### V3.5.0 iOS PWA 與敏感資訊可見範圍強化

本版維持現行角色與 Trip 瀏覽制度，不新增 Trip 公開／私人設定，也不建立 `trips.is_public` migration。主要範圍如下：

- 修正 iOS PWA 在管理／編輯欄位聚焦後保留放大倍率、退出後可左右移動的問題；表單實際字級不得低於 16px，退出管理時解除焦點並限制水平溢出。
- iOS standalone PWA 的 HTML 載入階段顯示專案圖示；Android 仍維持 V3.4.11 的單次原生 Splash 圖示流程，不得出現第二張圖示。
- 自駕／租車、領隊／導遊及一般其他資訊維持原有第二層卡片結構；進入管理編輯時預設為「一般資訊」，不新增可見範圍選項。
- 編輯者可按「新增敏感資料」快捷入口建立或編輯敏感卡片；此模式自動套用「僅行程管理者可查看」，不新增敏感資料夾或第三層分類。
- 「僅行程管理者可查看」只允許目前 Trip 被指派的 `trip_editor` 與 `super_admin` 讀取；Guest、User 與其他 Trip 的編輯者不得取得資料。
- 沿用 `other_info_items.allowed_roles` 並由 Supabase RLS 落實卡片讀取限制；不能只在前端隱藏。未授權角色不得保存敏感卡片快取，卡片改為限制後須在下次成功刷新時清除舊快取。
- 儲存敏感資料時才顯示二次提醒：訂房編號、訂位代碼、租車確認碼、私人電話、個資或受限文件連結，應使用敏感資料模式；一般公開網路連結可維持一般資訊，固定帳號保護的雲端文件仍屬敏感內容。這段文案不直接放在編輯畫面。
- Guest 不提供離線行程內容；登入且仍具權限的帳號才可使用依帳號隔離的離線資料。
- 本版不建立額外的「敏感附件」可見層級；固定帳號保護的雲端文件由外部雲端服務的帳號權限控管，卡片本身則依本版資訊可見範圍限制。
- 納入 V3.4.11 發現的 BUG024：其他資訊同步失敗修正、失敗提示手動重試、`trip_editor` 權限收斂，以及 `docs/sql/014_other_info_sync_deleted_rows.sql`；前端、013／014 migration 與驗證同屬 V3.5.0 發布範圍。013／014 已套用正式 Supabase 專案，發布前同步、權限與離線佇列驗證已通過；Android／Chrome PWA 與 iOS 列為發布後補驗證。
- 本版所有新增或調整畫面必須先製作模擬圖，經 Product Owner 確認後才可修改程式。
- V3.5.0 已發布並採 `FORCE_UPDATE = true`：本版同時包含敏感資訊 RLS、權限快取隔離與同步流程修正；舊版 PWA 升級及 Android／Chrome PWA 啟動／版面已通過，iOS 因目前無設備而保留未驗證並暫時跳過；發布後驗證已於 2026-08-26 確認結案。

### V3.5.1 Supabase Storage 附件安全維護、跨裝置即時刷新與 App 分享（不新增敏感附件可見層級）

- 將 `expense-attachments` bucket 與 `storage.objects` 政策納入可版本控制的 migration。
- 確認 bucket 為 private，並依 Trip 權限限制 INSERT、SELECT、UPDATE、DELETE。
- 驗證附件路徑中的 Trip ID，並評估簽名網址有效期限。
- 納入已確認的 App 分享功能：頁首於既有關閉按鈕左側新增小型分享圖示，點擊後開啟浮動視窗；既有 App 文案與小圖示不得變動。
- 浮動視窗依已確認模擬圖顯示本專案圖示、「我的旅行小幫手首頁」超連結（開啟預設瀏覽器）、「分享連結」文字，以及每行四個可上下滑動的原生分享 App 圖示。
- 點擊原生 App 圖示後使用系統分享機制，由使用者選擇通訊軟體頻道或其他分享目標；不新增「更多」按鈕、不建立單一 Trip 分享網址。
- 保留原本關閉按鈕，點擊後直接返回當下 App 主畫面；新增功能僅能變動本次確認的分享部分，不能改動既有文案與小圖示。
- 修正其他資訊、領隊／導遊資料及共同準備清單的跨裝置即時刷新缺失：同一 Trip 的另一台裝置收到新增、修改、勾選或軟刪除事件後自動重新載入。
- 以 Supabase Realtime 監聽 `other_info_items`（限定 `trip_id`），收到事件後透過 RLS-safe query refetch；軟刪除以 UPDATE 事件處理，不直接顯示未驗證的事件 payload。
- 共同準備清單同步監聽 `checklists`（限定 `trip_id`、`scope = shared`）及其 `checklist_items`（限定 shared checklist ID），涵蓋新增、修改、勾選、刪除／軟刪除與排序變更。
- 補上不同帳號同時修改同一共同清單項目的競態驗收：同一欄位採最後成功雲端寫入結果；離線／結構修改沿用三方合併，pending 不得被遠端事件清除或覆蓋。
- 保留 focus／visibility／reconnect fallback；重新載入不得覆蓋尚未送出的本機同步佇列，並補上 Realtime publication／migration 與雙裝置驗證。
- 屬既有功能的安全補強，使用 Patch；是否強制更新依舊版相容與暴露風險決定。
- 已於 2026-08-26 完成發布並結案：正式 Supabase migration、Storage 四角色 RLS、宜蘭附件、跨裝置即時刷新與共同清單競態回歸均已通過。

### V3.5.2 Supabase 資料庫權限函式硬化（已完成，無獨立 App 發布）

- 盤點 `SECURITY DEFINER`、固定安全 `search_path`、收斂公開執行權限。
- 確認 `public` schema 建立物件權限，執行 database advisors 並回歸所有主要資料權限。
- 屬資料庫安全維護，使用 Patch。
- 已完成 migration `20260826083326_v352_database_function_hardening.sql`：高權限實作移至非暴露的 `private` schema，`public` 保留同名 `SECURITY INVOKER` 包裝以維持既有 RLS 參照；所有 `tc_` 函式固定空 `search_path`，函式執行與未來預設權限採明確白名單。
- 已正式套用 migration（Supabase migration `20260826083326`），並完成套用後函式權限驗證、security／performance advisors、核心角色／RLS 與 Storage 角色回歸；本版相關函式安全 advisors 已清除。
- Product Owner 已確認本版不建立 App build、Git 版本標籤或 App 版本 metadata；V3.5.1 前端可繼續使用，`FORCE_UPDATE` 不適用。資料庫 migration 與完成紀錄永久保留，下一個 App 候選版本直接進入 V3.5.3。

### V3.5.3 前端瀏覽器安全防護（已發布）

- 已確認使用 Patch 版，不修改 Supabase schema、RLS、角色矩陣或既有 Offline First 資料流。
- 正式 build 加入 Content Security Policy，Supabase API／Realtime 來源依環境產生；`script-src` 不開放 `unsafe-inline` 或 `unsafe-eval`。
- 加入 `no-referrer`、OAuth 來源驗證、安全 DOM、HTTP／HTTPS URL 驗證及外部視窗 opener 隔離。
- GitHub Pages 無法提供自訂 Permissions Policy、CSP Report-Only 或 `frame-ancestors` header，本版不加入無效 meta。
- lint、正式 build 與 postbuild 安全檢查已通過；一般瀏覽器 OAuth、PWA 安裝／離線、附件、Excel、分享與外部連結實機回歸已通過；iOS standalone PWA OAuth 依 Product Owner 決定列入日後補驗證，不阻擋本次發布。
- 詳細修改邊界及驗證矩陣見《17_V3.5.3_前端瀏覽器安全防護驗證》。
- 已於 2026-08-26 合併、建立 Git 版本標籤 `v3.5.3` 並發布；正式站的新版本更新提示已於 2026-08-27 補驗證通過；iOS standalone PWA Google OAuth 保留日後補驗證。

### V3.5.4 管理者帳號名稱映射與新增旅程預帶入（已發布）

- 新增 `admin_profiles` 作為管理者帳號設定來源，與既有僅管理角色／Trip 指派的 `admin_users` 分離。
- `admin_profiles` 保存 Supabase Auth 使用者識別、目前 Email、固定顯示名稱、是否預設帶入新增旅程及排序；Email／名稱調整後由 App 讀取最新設定。
- 新增旅程時，App 依 `include_in_new_trip` 與 `sort_order` 預帶入「名稱=Email」參與者；可編輯者 Email 欄位維持空白，只顯示範例。
- `admin_users` 維持權限來源，不以顯示名稱作授權判斷；profile table 需具最小 RLS，只有 `super_admin` 可維護設定。
- 此項包含 schema、RLS、既有管理者資料初始化與表單行為回歸，獨立為 V3.5.4，不插入已進入驗證階段的 V3.5.1。
- V3.5.4 起新建 Trip 的 ID 依初次設定的旅程型態與出發開始日期固定產生：自由行為 `free-travel-YYYY-MM-DD`，跟團為 `group-tour-YYYY-MM-DD`；月、日皆補零為兩碼。
- 同一型態與同一初始出發開始日期只允許一個 Trip；新增 Trip 必須在連線狀態下進行，建立前必須同時檢查本機與 Supabase 既有 ID，重複時阻擋建立並提示使用者調整初始出發開始日期或型態。既有 Trip 的離線瀏覽與既有可離線操作不受此限制影響。
- 既有 Trip ID 一律不搬移、不重命名；新建後的 ID 亦不因後續編輯名稱、型態或出發日而改寫，避免破壞帳目、清單、Storage、RLS 與離線快取關聯。
- 正式資料庫與 Super Admin 介面驗收已通過；發布 metadata 日期為 2026-08-26、採非強制更新。已於 2026-08-27 合併、建立 Git 版本標籤 `v3.5.4` 並部署。

#### 新增旅程 ID 檢核流程

1. `super_admin` 點選「新增旅程」。
2. App 先確認目前網路連線狀態；離線時不得開啟建立流程，並提示「新增旅程需要網路連線」。
3. 管理者填寫旅程資料，包含旅程型態及初次設定的出發開始日期。
4. App 依型態及初始出發開始日期計算候選 ID：自由行為 `free-travel-YYYY-MM-DD`，跟團為 `group-tour-YYYY-MM-DD`。
5. 管理者按下「儲存／建立旅程」後，App 先檢查本機快取、再查詢 Supabase 是否已有候選 ID。
6. 任一檢查發現相同 ID 時，禁止建立；不寫入本機或 Supabase、不關閉表單、不切換目前旅程，並保留所有已填欄位，提示管理者調整初始出發開始日期或型態後再送出。
7. 兩處均無相同 ID 時，才建立本機資料並以 Supabase `insert` 新增雲端資料；建立新 Trip 不得使用可覆寫既有資料的 `upsert`。
8. 若多位管理者同時建立相同候選 ID，資料庫 `trips.id` 主鍵為最後防線：先成功者完成建立，後送者收到重複 ID 錯誤，App 顯示與第 6 點相同的阻擋提示，且不保留新 Trip 本機資料。
9. 建立成功後，Trip ID 固定；後續編輯旅程名稱、型態或出發開始日期時，不重新產生、不重新檢查，也不改寫 Trip ID。

### V3.5.5 行程返回、外部連結與資訊文字醒目（已發布；手機實機驗證延後）

- 修正刪除目前行程後的首頁選取邏輯：返回 App 主頁時，必須套用與進入 App 時完全相同的既有預設選取規則；不得停留在已刪除行程或任意選取其他行程。
- 資訊卡片內的 HTTP／HTTPS 超連結改採系統外部瀏覽器開啟；避免在 App 內嵌網頁環境中載入，並維持 URL 驗證、`noopener` 與 `noreferrer` 安全限制。受 iOS／Android 平台限制時，須優先交由系統預設瀏覽器處理，並明確驗證實際開啟目的地。
- 資訊卡片中的 Google Maps 網址須優先嘗試開啟裝置已安裝的 Google Maps App；未安裝或系統不支援 deep link 時，再安全回退至外部瀏覽器的 Google Maps 網頁。
- 資訊卡片內容欄及每日詳細行程的「說明」欄改為支援選取文字後套用指定文字顏色；只改變所選字元，不提供整欄統一顏色設定。原有純文字、換行、網址辨識、離線儲存、同步與角色可見性必須維持相容。
- 本版屬既有行為修正與小型體驗改善，使用 Patch 版號；不修改 Supabase schema、RLS、角色矩陣或既有 Offline First 資料流。完整規格與操作示意見《19_V3.5.5_行程返回外部連結與資訊文字醒目規格》。
- Product Owner 已於 2026-08-27 完成合併與部署；電腦版驗證已通過，Android／iOS 手機實機驗證保留日後補驗證。

### V3.5.6 版本更新最低支援版本機制與手機文字選色工具列修正（已發布）

- 修正舊版 App 只讀取最新 `forceUpdate`、因而可能略過中間必要更新的版本政策缺口。
- 導入 `minimumSupportedVersion`，以執行中的 `APP_VERSION` 判斷對目前客戶端是否必要更新；已達最低版本者可保留一般更新的「稍後更新」。
- V3.5.6 為橋接版；公開 metadata 在橋接期間保留 `forceUpdate: true`，使不認識新欄位的歷史 App 仍被要求更新，新版則依最低支援版本得到正確的條件式結果。
- 必要更新不受已讀版本、稍後更新、PWA 顯示模式或一般桌面瀏覽器限制；離線且低於最低支援版本時必須阻擋並提供重試。
- 修正 V3.5.5 手機長按選字後，系統原生選取選單遮住文字顏色工具列的問題：將「A」選色按鈕與提示詞整塊由文字輸入框上方移到下方，保留原生反白、選取控制點及既有套色行為，不加入清除反白處理。
- 修正全形冒號行程時間無法參與排序的問題：接受 `08:00` 與 `08：00` 並在儲存時統一為 `08:00`，但不接受 `08 : 00` 或 `08 ： 00` 等冒號旁含空白的格式；既有全形冒號資料不需重新編輯即可正確排序。新增／編輯時，非空白時間若包含非數字、錯誤符號或無效時分，須提示使用者修正、保留表單內容並禁止儲存，不得讓格式錯誤資料進入排序。
- 不調整 Trip、Supabase schema、RLS、角色或資料格式。完整方案見《20_V3.5.6_版本更新最低支援版本機制》。
- Product Owner 已於 2026-08-28 完成合併、建立 Git 版本標籤 `v3.5.6` 與部署，發布前驗證通過。

### V3.6.0 地點間預估移動資訊

- 在每日行程相鄰地點間顯示交通方式、預估時間與距離，支援開車、步行及大眾運輸。
- Trip Editor／Admin 查詢並儲存；Guest／User 只查看已儲存結果。
- 這是新的主要使用者功能，安排於 V3.6.0。
- 正式 Supabase migration、`020`、advisors 與 `travel-route` Edge Function 已部署驗證；Google Cloud 的 Places API (New)、Routes API、Server Key API 限制、初始配額與 US$5 月預算警示已核對。
- `http://localhost:4173/Travel-Companion/` 已以正式登入帳號完成 V3.6.0、Supabase 資料載入及 Routes 查詢，且確認瀏覽器 bundle／Network 不含 Google key。
- 已於 2026-08-30 合併至 `main`（`0cce637`）、建立 Git 版本標籤 `v3.6.0` 並部署 GitHub Pages；正式 metadata、V3.6.0 Service Worker 接管、版本顯示、Guest 行程與已存路線均已驗證。完整角色矩陣、正式站 OAuth 自動化登入與 Android／iOS 實機保留發布後補驗證。
- 完整規格見《15_V3.6.0_地點間預估移動資訊規格》，測試與發布順序見《21_V3.6.0_發布前測試與正式發布流程》。

### V3.6.1 Android 記帳金額欄位與 PWA 更新流程修正（已發布）

- 修正 Android Chrome 與 PWA 聚焦新增記帳「金額」欄位後，原生數字輸入控制項超出卡片右側的問題。
- 新增與編輯金額列統一採可縮小的兩欄版面，金額欄必須受父層寬度約束；不得用禁止縮放或裁切內容代替修正。
- 修正手機與桌面第一次按下「馬上更新」時，新版 Service Worker 尚未完成下載便提前結束流程，必須第二次點擊才執行更新的競態。
- 更新流程必須等待新版 Service Worker 進入可接管狀態後，自動完成單次接管與重新載入；不得要求使用者再次點擊，也不得提前隱藏必要更新提示。
- 保留既有金額資料格式、儲存、千分位顯示、附件與付款人流程，不需要資料庫或雲端設定變更。
- Android 手機瀏覽器／PWA 為必要重現與驗收環境；iOS Safari／standalone PWA、桌面瀏覽器／PWA 一併回歸，並驗證更新只需點擊一次。
- 已於 2026-09-01 完成合併、建立 Git 版本標籤 `v3.6.1` 並部署；Android 手機實機確認更新提示不需點擊兩次，記帳金額輸入欄位不再變形或超出畫面。
- 發布後觀察到有更新時，重新顯示使用者介面的體感速度偏慢；目前尚未量測並定位原因，不先判定為重新載入機制造成，列為後續效能檢查。
- 完整規格見《23_V3.6.1_Android記帳金額欄位版面修正規格》。

### V3.6.2 行程天數縮短刪除與歷史行程鎖定（已發布）

- 編輯既有行程並縮短天數時，不在輸入階段刪除資料；按下儲存後先顯示二次確認，明列原天數、新天數、所有將被移除的 Day，以及各 Day 的行程卡片與路線資訊數量。
- 確認後直接永久刪除超出新天數的所有 `daysData`；不封存、不建立復原區，也不影響共同清單、帳本、其他資訊或其他非每日行程資料。
- 必須支援一次縮短任意 N 天；一併刪除連續尾端 Day，確認畫面可捲動列出全部受影響日期，不得只處理最後一天。
- 依台灣時間判斷歷史行程：行程最後一天結束後，翌日 `00:00` 起，`trip_editor` 對整個歷史行程的共用資料全面唯讀；只有 `super_admin` 可繼續修改。使用者介面一律顯示中文「管理者」，不顯示角色代碼。
- 天數縮短影響摘要與二次確認只在變更行程天數時觸發；其他欄位編輯與刪除整個行程不新增本次確認。非歷史行程沿用既有角色規則；歷史行程則套用上述 `trip_editor` 全面唯讀、管理者可編輯規則。
- 除上述防護、欄位狀態與必要警示文案外，不變更其他畫面、資料格式、權限或同步流程。
- Product Owner 已確認本版採必要更新；正式 metadata 為 `minimumSupportedVersion: 3.6.2` 與舊版橋接用 `forceUpdate: true`，避免舊前端在新 RLS 下持續產生會被拒絕的歷史共用資料同步。
- 2026-09-01 已核准 UI，完成前端、Supabase migration、專項驗證與 build；migration 已套用至已連結 Supabase 專案，回滾式角色驗證 9 項全數通過。
- Product Owner 已完成合併至 `main`（`e9333f5`）、建立並推送 Git 版本標籤 `v3.6.2` 及手動部署；發布狀態為「已發布」。發布後功能驗證與桌面、Android 矩陣已通過；iOS Safari／standalone PWA 尚未測試。
- 完整規格見《24_V3.6.2_行程天數縮短刪除與歷史行程鎖定規格》。

### V3.6.3 V3.6.2 發布後前端修正（已完成程式實作，待發布驗證）

- V3.6.2 的縮短天數、台灣日期邊界、歷史角色、私人與既有規則、離線／同步，以及桌面與 Android 矩陣已通過；iOS Safari／standalone PWA 尚未測試。
- 納入 BUG029：更新提示正確顯示 V3.6.2 為必要更新，但更新後版本資訊錯顯示「一般更新」。
- BUG029 已確認是「客戶端目前是否仍需更新」與「版本本身的發布政策」共用同一判定所致；V3.6.3 將分離兩種資料來源。
- 更新提示繼續依 `isMandatoryForCurrentClient` 控制；版本資訊與版本歷史改讀取該版本保存的發布政策，不因使用者已完成更新而改變。
- 同版處理交通方式圖示、管理者歷史提醒時機，以及縮短天數改為兩個不同文案的確認階段。
- 本版只處理前端顯示與確認流程；歷史唯讀參與者及跨裝置 Trip 資料變動提醒已拆至 V3.6.4。
- 本版維持 Patch；Product Owner 已確認採一般更新，`minimumSupportedVersion` 維持 `3.6.2`，`forceUpdate: false`。
- 四項前端修正、自動驗證與 V3.6.3 發布候選 metadata 已完成；裝置回歸與發布作業尚待完成。
- 完整規格見《25_V3.6.3_V3.6.2發布後前端修正規格》。

### V3.6.4 歷史唯讀參與者與跨裝置資料變動提醒（已立案，開發前再確認）

- `trip_editor` 若同時是目前歷史 Trip 的參與者，該 Trip 進入「歷史唯讀參與者」有效狀態；這不是 User，也不修改帳號或 `admin_users` 的全域角色。
- 保留完整歷史共用資料讀取能力，不顯示 `trip_editor` 專用的「歷史行程已鎖定」提醒，並繼續由前端與 Supabase 禁止所有歷史共用資料寫入。
- 此狀態只適用目前歷史 Trip；其他受指派的非歷史 Trip 仍保留 `trip_editor` 權限，非參與者的歷史 `trip_editor` 與管理者沿用 V3.6.2 規則。
- 其他設備新增或修改 Trip 資料時，只提示使用者手動刷新，不自動刷新或捨棄編輯中資料。
- 資料變動提醒的事件範圍、UI、刷新粒度、未儲存狀態、離線備援、事件來源、權限隱私、migration、相容性與更新政策，必須在開發前再次討論並核准。
- 完整規格見《26_V3.6.4_歷史唯讀參與者與跨裝置資料變動提醒規格》。

### V3.6.5 PWA 更新後介面顯示效能改善（已核准，待實作）

- V3.6.1 的單次點擊更新競態已修正並於 V3.6.2 回歸通過；本版不變更 `SKIP_WAITING`、`controllerchange`、單次 reload 或更新提示安全流程。
- 可移除量測已拆分 Worker 接管、reload navigation、Trip 初始化與啟動畫面移除；本機桌面樣本顯示按鈕至接管約 38 ms，主要等待在更新後首次 Service Worker navigation、重複 Trip 雲端讀取及資料 ready 後仍等待 lazy chunk。
- 已核准改善一：`isSessionReady && !isLoading` 後立即移除靜態啟動畫面，lazy 功能頁尚未完成時顯示既有載入 fallback。
- 已核准改善二：同一次 App 初始化共用 Trip 雲端快照，避免旅程清單與初始詳細資料重複讀取完整雲端 Trip；不得建立跨頁面或跨帳號長效快取。
- 不繞過約 1 秒的瀏覽器／Service Worker 首次 navigation 等待，以免重新引入舊 App shell、空白頁、新舊 bundle 混用或二次更新問題。
- 改善前後須以相同環境各量測至少 5 次，並回歸角色、登入、離線、pending、跨裝置、單次接管及單次 reload。
- 本版維持 Patch；是否採必要更新，待實作完成與風險驗證後由 Product Owner 決定。
- 完整規格見《27_V3.6.5_PWA更新後介面顯示效能改善規格》。

### V3.7.0 當日後續行程時間連動調整

- 2026-08-31 已完成開發內容、互動流程、文案、進位規則、錯誤處理與儲存邊界定案，可進入實作。
- 在 Day 行程管理新增單次「調整時間」模式，不使用會持續保留的永久開關，也不改變一般卡片編輯／儲存行為。
- 管理者選擇「從這站開始」並輸入新離開時間後，依有效路線時間與各站原停留時間計算當日後續到達／離開時間；每站推算到達時間無條件進位至下一個 `00` 或 `30` 分刻度，再加上原停留時間。
- 以景點與交通區段交錯的逐行文字預覽呈現；按鈕為「取消調整」與「套用調整」。
- 交通圖示沿用 V3.6.3 完成的自託管 Google Material Symbols，並保留文字與可存取名稱。
- 缺少／過期路線、無效時間、跨午夜或大眾運輸需要重查時停止計算；整批更新全部成功才寫入，不允許部分儲存。
- 完整規格見《22_V3.7.0_當日後續行程時間連動調整規格》。

### 安全方向相容性檢查（未經確認不得移除）

- V3.5.1 Storage 安全維護與 V3.5.0 方向相容：只強化 bucket、Storage RLS、附件路徑與簽名網址，不新增敏感附件分類或可見層級。
- V3.5.2 資料庫權限函式硬化與 V3.5.0 方向相容：強化 `SECURITY DEFINER`、`search_path` 與公開執行權限，不改變既定角色矩陣或 `allowed_roles` 規則。
- V3.5.3 未修改 Supabase、角色與同步資料流；正式 build、OAuth、PWA、Service Worker、附件與 App 分享外部連結回歸均已通過。iOS standalone PWA OAuth 保留為日後補驗證。

## 暫不排程

- 預約紀錄、行李清單、旅行日誌、保險資訊與 AI 助手，須由 Product Owner 另行立案。
- 帳本附件管理及離線載入效能改善，待依實際範圍安排 Patch 或 Minor。
