# V3.6.4 實作與驗證紀錄

> 日期：2026-09-04
>
> 狀態：發布候選已完成；production migration、正式部署與實體裝置驗證尚未執行
>
> 基準提交：`dc984f9`（`develop`）
>
> 候選版本：V3.6.4，`minimumSupportedVersion: 3.6.4`、`forceUpdate: true`

## 實作摘要

- 「歷史唯讀參與者」以目前 Trip 的 `trip_editor` 指派、台灣歷史日期及正規化參與者 Email 共同判定；維持原角色與完整歷史共用資料讀取，只取消重複鎖定橫幅。
- 每次 App 啟動建立記憶體內 UUID，透過自訂 request header 寫入修訂來源；不保存帳號或裝置識別資訊。
- 新增單列 `app_data_revision`、Trip／`trip_editor` trigger、最小 RLS／GRANT 與帳號專屬 private Broadcast policy。payload 只有 `revision`、`updated_at`、`source_client_id`。
- Trip 主資料使用載入時的 `updated_at` 做條件式 UPDATE；零筆更新視為版本衝突，停止儲存及後續編輯者清單同步。
- 一般遠端更新提供持續橫幅與「稍後」狀態；只鎖 Trip 主資料，既有 Travel Tool 規則不變。Trip 刪除、目前 Trip 撤權及版本衝突使用不可關閉對話框。
- 撤權／刪除只清目前 Trip 的角色、授權、受限制共用快取及共用 pending；私人帳本、私人清單與其他 Trip 不清除。
- 欄位文案改為「記帳代號設定（不授予本行程編輯權）」與「可編輯者 Google Email」。

## 自動化與靜態驗證

| ID | 環境／角色 | 前置與操作 | 預期 | 實際結果與證據 | 結果／清理 |
|---|---|---|---|---|---|
| A01 | Windows、Node 22.23.2；不涉及帳號 | `npm run build` | 版本歷史、更新政策、既有 V3.6.2／V3.6.3、V3.6.4、TypeScript 與 Vite production build 全部通過 | 2026-09-04 完整 build 通過；Vite 轉換 1,896 modules 並產生 V3.6.4 assets | 通過；只產生被忽略的 `dist` |
| A02 | Windows；不涉及帳號 | `npm run lint`、`git diff --check` | 無 ESLint error／warning、無 whitespace error | 兩項均無錯誤 | 通過；無資料變更 |
| A03 | Node；模擬 own／other source | `npm run verify:v364-trip-data-revision` | own `+1` 不提醒；own gap、other source 提醒；延遲／重複忽略；固定安全結構與文案 | 專項腳本輸出「V3.6.4 …驗證通過」 | 通過；無資料變更 |
| A04 | Node；不涉及帳號 | `npm run verify:browser-security` | CSP、Referrer Policy、外部開啟與 OAuth DOM 規則不回歸 | 腳本輸出瀏覽器安全驗證通過 | 通過；無資料變更 |
| A05 | 靜態 migration review | 檢查 migration 與 rollback SQL | 單列 constraint、RLS、SELECT-only grant、private Broadcast、六個 trigger、trigger-only functions 不開放瀏覽器角色 | `scripts/verify-v364-trip-data-revision.mjs` 固定檢查；`022` 提供交易式角色驗證，`023` 提供回滾 SQL | 靜態通過；尚未套用 production |
| A06 | Supabase CLI 2.115.0、已連結 production | `npx supabase db push --linked --dry-run --skip-vault` | 不變更資料庫，只列出待套用 migration | 僅列出 `20260904105703_v364_trip_data_revision.sql`，回報 `dryRun: true` | 通過；production 無變更 |
| A07 | Supabase CLI、已連結 production | `npx supabase db lint --linked --schema public,private,realtime --level error --fail-on error` | 建立 migration 前既有 schema lint 基準 | 唯一 error 為 Supabase 內建 `realtime.apply_rls` 的 `XX000 expression does not return data`；不是 V3.6.4 物件，且 migration 尚未套用 | 已記錄既有基準；不得誤稱全庫 lint 通過 |

## 瀏覽器驗證

| ID | 瀏覽器／viewport／角色 | 操作與預期 | 實際結果 | 結果／證據與清理 |
|---|---|---|---|---|
| B01 | Codex in-app Chromium、預設 viewport、Guest | 開啟 `http://127.0.0.1:5173/Travel-Companion/`，等待 Session／Trip 載入；首頁、Trip、行程與側邊導覽可用 | App 正常載入既有雲端 Trip，Guest 唯讀標示與每日行程正常 | 通過；測試分頁已關閉 |
| B02 | 同上、390×844、Guest | 驗證手機寬度、Header、側邊選單與共同清單唯讀；頁面不得水平溢出 | `innerWidth=390`、`scrollWidth=390`；共同清單顯示唯讀提示且操作 disabled | 通過；viewport 已 reset，測試分頁已關閉 |
| B03 | 同上、390×844、Guest | 檢查 console error | 首輪發現 `SortableCard` list child 缺少 `key`；補上 item `key` 後完整重測，console errors 為 `[]` | 修正後通過；保留原失敗原因與重測結果 |
| B04 | Codex in-app Chromium、Vite production preview、390×844／預設 viewport、Guest | 直接開啟 production build，確認版本、資料載入、水平溢出與 console | 顯示 `v3.6.4`；雲端 Trip 與每日行程載入完成；390px 時 `scrollWidth=390`；console errors 為 `[]` | 通過；viewport 已 reset、preview 已停止、測試分頁已關閉 |

## 尚待 Codex 執行的發布閘門

以下不是 Product Owner 手動測試；必須在可用的隔離／本機 Supabase 或依核准順序套用 production migration 後，由 Codex 執行並補回本文件：

- 執行 `docs/sql/022_v364_trip_data_revision_validation.sql`，完成角色、修訂遞增、request header、policy 與函式權限的交易式驗證並 `ROLLBACK`。
- 完整測試 Trip 新增／修改／刪除、指派新增／修改／移除，以及排除的帳本、附件、共同清單、其他資訊、換匯與私人資料事件。
- 兩個隔離 Session 的 `updated_at` 競態、own-source／gap／400ms 合併、topic 偽造、前端 Broadcast 寫入拒絕、撤權最後訊號與後續停止。
- 執行 migration 後的 Security／Performance Advisors，僅處理或記錄 V3.6.4 新增 finding。
- 演練 `docs/sql/023_v364_trip_data_revision_rollback.sql`；正式事故處理仍以新 migration 前進，不改寫已套用 history。
- 依「資料庫 → App assets → `app-version.json`」順序完成 V3.6.3 相容窗口與正式必要更新驗證。

本機無法執行上述資料庫整合，是因 Docker Desktop Linux engine 未啟動；本次未以 production 取代隔離測試，也未套用 migration。

## Product Owner 只需手動驗證

只有下列項目需要實體設備；其餘已完成或仍由 Codex 負責：

1. iPhone／iPad 的 Safari 與 standalone PWA：從 V3.6.3 執行必要更新，測背景／鎖定恢復、飛航模式重連、安全區域、鍵盤、觸控與 reload 後白畫面／舊介面。
2. Android 的 Chrome 與已安裝 PWA：測必要更新、背景恢復、完全關閉重開、離線重連、系統返回鍵、觸控與版面。
3. 兩台實體裝置：一台修改 Trip，另一台確認一般提醒與「稍後」只鎖 Trip；再測撤權不可關閉提示、刪除目前 Trip 後 reload 與切換可用 Trip。
4. 實際使用感受：提醒是否清楚、按鈕是否容易誤按，以及 reload／更新後是否卡住。

### Product Owner 實測回報

| ID | 日期 | 環境／範圍 | 實際結果 | 狀態／後續 |
|---|---|---|---|---|
| PO01 | 2026-09-04 | 電腦版、本機 V3.6.4 production preview（`http://localhost:4173/Travel-Companion/`） | Product Owner 回報通過 | 通過；本項為補充實測，不取代尚待 Codex 完成的資料庫與發布閘門 |
| PO02 | 2026-09-04 | iOS Safari／standalone PWA | 未執行 | 待測；不得列為通過 |
| PO03 | 2026-09-04 | Android Chrome／已安裝 PWA | 未執行 | 依 Product Owner 決定延至正式發布後測試；不得列為通過 |
| PO04 | 2026-09-04 | 兩台實體裝置跨裝置提醒、稍後鎖定、撤權與刪除流程 | 未執行 | 待資料庫 migration、Codex 發布前驗證與正式 HTTPS 部署完成後測試 |

本次回報未包含裝置型號、瀏覽器版本、截圖或錯誤證據；電腦版結果以 Product Owner 文字回報為依據。正式放行仍須完成 iOS、Android 與兩台實體裝置項目。

## 發布與回滾狀態

- production migration：未套用。
- GitHub Pages／Service Worker：未部署 V3.6.4。
- 正式 `app-version.json`：repository 候選已更新，線上 metadata 未變更。
- 回滾檔：`docs/sql/023_v364_trip_data_revision_rollback.sql`；尚待可用資料庫環境演練。

完整需求與放行規則見《26_V3.6.4_歷史唯讀參與者與跨裝置資料變動提醒規格》。
