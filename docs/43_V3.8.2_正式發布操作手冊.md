# V3.8.2 正式發布操作手冊

> 狀態：已完成 V3.8.2 開發、驗證、正式發布與 GitHub Pages 部署；目前已切回 `develop`
>
> 正式發布日期：2026-09-11
>
> 發布政策：一般更新；`minimumSupportedVersion: 3.8.1`、`forceUpdate: false`

## 對外更新提示

標題與政策文案沿用既有更新元件：

> 發現新版本
>
> 有新版本可供更新

本次更新內容：

- 跨日複製行程時，現在可先設定副本的抵達與離開時間，並一次套用到所有所選 Day。
- 副本會依新的抵達時間插入正確位置，避免未填時間而落到當日行程最下方。

本版為一般更新，更新提示保留「稍後更新」及「立即更新」。

## 正式設定

- `package.json`、`package-lock.json`、`src/config/appVersion.ts` 與 `public/app-version.json`：`3.8.2`。
- `PREVIOUS_RELEASE_VERSION = "3.8.1"`，V3.8.1 已加入 `versionHistory.ts`。
- `RELEASE_DATE = "2026-09-11"`。
- `IS_MANDATORY_RELEASE = false`、`MINIMUM_SUPPORTED_VERSION = "3.8.1"`、`FORCE_UPDATE = false`。
- 本版沒有 Supabase schema、RLS、Edge Function 或資料 migration。

## 發布前驗證

- [x] 跨日複製與時間格式領域測試。
- [x] TypeScript、ESLint、完整 build、PWA build 與瀏覽器安全檢查。
- [x] 桌面 1280×900 與 390×844 手機版的未填／有效時間狀態。
- [x] 全形冒號輸入、送出條件、較早／相同／較晚插入、多日獨立 ID 與路線快取失效。
- [x] `git diff --check` 與候選工作區差異檢查。

## 正式發布結果

- [x] 候選提交 `c321964` 已推送至 `origin/develop`。
- [x] `develop` 已快轉合併至 `main`，並在 `main` 重跑 lint 與完整 build。
- [x] `origin/main` 已推送，annotated tag `v3.8.2` 已建立並推送。
- [x] `npm run deploy` 已將 `dist` 部署至 GitHub Pages。
- [x] `gh-pages` 發布提交為 `599cd893814bebce6b449168110d7d6826b571b4`。
- [x] 正式 `app-version.json` 已確認為 `3.8.2`、`minimumSupportedVersion: 3.8.1`、`forceUpdate: false`，日期與兩項更新內容正確。
- [x] 正式首頁引用 `assets/index-DEj2_xMN.js` 與 `assets/index-D4MvHaVt.css`。
- [x] 正式站 Guest 模式可載入，版本資訊顯示 V3.8.2、一般更新與兩項核准文案。
- [x] 發布完成後已切回 `develop`，且 `develop`、`main` 與 `v3.8.2` 發布點一致。

## 發布後補驗

- [ ] Android／iOS 實體裝置安裝／更新及 Service Worker 接管。
- [ ] 已登入管理者在正式站抽查跨日複製到單日／多日；來源 Day 不變且目標 Day 依新時間插入。
- [ ] 多裝置版本衝突或寫入失敗時，確認所有目標 Day 均維持原狀。
- [ ] 發現資料安全、白畫面、更新接管或跨裝置一致性問題時，建立向前修復版本，不改寫 V3.8.2。
